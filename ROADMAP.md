# 待办与交接

> 写给**在另一台机器上克隆本仓库后接手的人**(含 AI 助手)。
> 动手前请先完整读 [ARCHITECTURE.md](ARCHITECTURE.md) —— 那里是工程约定与架构铁律,
> 本文只写「接下来做什么、以及为什么不做某些事」。
>
> 最后更新:2026-08-04

---

## 0. 新机器上先做这四件事

```bash
npm ci
npm run build          # 类型检查 + 构建(4 份 tsconfig 一起过)
npm test               # 单元测试(含 server/)
npm run lint-content   # 卡池结构性自检
```

四条都绿,说明环境是好的。**不需要克隆任何别的仓库**:
`src/` 一行都不 import 外部素材源,生成产物(`src/content/generated/`)已入库。

> **Windows 上跑得通了**(2026-08-04)。此前 `npm test` 在 Windows 必红三条,
> 报 `ENOENT ... 'C:\C:\...\All%20Codes\...'` —— 三个扫源码的测试用
> `new URL('.', import.meta.url).pathname` 取目录,而它在 Windows 上给的是
> `/C:/…/All%20Codes/…`:多一个前导斜杠、空格还是 `%20`。macOS 上照跑,
> 所以这一类**只在换机器时才炸**,而这份文档正是写给换机器的人的。
> 已统一改成 `dirname(fileURLToPath(import.meta.url))`(仓库里 `purity.test`
> 等一直是这种写法)。注意 `readFileSync(new URL(...))` 传 URL **对象**是安全的,
> Node 自己会解码 —— 坏的只有 `.pathname` 那个字符串。

### 唯一的例外:素材源头

`npm run import-content` / `check-generated` / `export-portraits` 依赖同级目录的
`../ThreeKingdomMastersIOS`(另一个项目,只读的素材源)。**这台机器上没有它是正常的** ——
那三条命令会自己跳过或报找不到,其余一切照常。

**没有它就不要动 `scripts/import-content.ts` 与 `scripts/seed-mechanics.ts`** ——
改了也没法重新生成,更没法验证。这两个文件的改动必须在有素材源的机器上做。

---

## 1. 当前状态(接手前必读)

| | 数值 |
|---|---|
| 卡池 | 2,434 张(可收集 2,416,其中武将 2,258) |
| 测试 | 1026 通过 / 117 个文件(含六份闸门自检,见第 37 条) |
| 首屏 | 356 / 400 KB gzip(主包 159.6/190 · 内容层 128.7/150 · 第三方 60.2/75) |
| 机制重复度 | 29.2%(最大同质组 12 张) |

### ⚠️ 闸门的现状(2026-08-04 更新)

**三道全绿。** 但这一轮真正的产出不是「把红变绿」,而是**两道闸门自己是坏的**:

- **`npm run sim-balance` 绿** —— 六套预组总胜率全部落在 40–60%(坐斷東南 42.6% 最低)。
- **`npm run sim-campaign` 绿** —— 此前那条红是**噪声**,判定撑不起自己的样本量。见下。
- **`npm run sim-hero-mirror` 绿** —— 此前那 4 条红是**尺子坏了**(备选主公全程后手),
  修好后只剩朱熹一个真出界,已按实测调回。见下。

另外两道**不在上面三道之列、但同样在 main 上红着或失灵**的,一并修了:

- **`npm run check-offline` 在 main 上是红的** —— 报 5 项假缺失。
  根因是底图那次 JPG → WebP 迁移只做了一半(CSS 与 vite.config 改了,
  而 import-content 的拷贝表、ARCHITECTURE、check-offline 三处都没改)。
  假缺失和真缺失混在一起,这道闸门实际上已经不能用了。
- **`npm run perf-budget` 的 chunk 基线能默默失效** —— 正则失配时只 `console.warn`,
  打包分块一改名它就不再守任何东西,而 CI 照样绿。已改成硬失败。

### 五道平衡模拟全部并行化(2026-08-05)

跑一遍全套 **39 分钟 → 11 分钟**。公共外壳在 `scripts/parallel.ts`(保序 worker 池)。

```
sim-balance      289s → 52s   5.5x
sim-hero-mirror  510s → 99s   5.1x
sim-cards        481s → 131s  3.7x
sim-campaign     725s → 250s  2.9x
sim-firstplayer  316s → 141s  2.25x
```

**并行不影响任何一个数字**:每局的种子只由索引决定、引擎是纯函数、
局与局之间零共享状态(铁律 1),所以按索引装回去就与串行逐位一致。
每一道接入时都做过**并行/串行逐格对拍**,`JOBS=1` 保留作单线程对拍开关。

**切分粒度要够碎**,同一个教训踩了两次:任务少而不齐时,尾部一堆线程在等最慢的那个
(sim-campaign 按关切只有 2.0x → 按局段切才 2.9x;sim-firstplayer CHUNK=60 只有 1.9x
→ CHUNK=20 才拉开)。只有 `sim-cards` 天然等长,不必再切。

意义不在「快了几倍」,而在**样本量不再需要权衡** —— 这一轮踩的坑一大半根子都是
「样本量不够又懒得加」,现在 `GAMES=1000` 也就几分钟。

---

一句话总结这一轮:**红的那几道没有一道是内容的问题,全是量它们的工具的问题。**
而根因高度一致 ——「有人发现过、写进注释、然后就没有然后了」,
或者「一次迁移只改了一半,剩下的地方悄悄失灵」。
所以修法一律是**把不变量钉成可执行断言**,不再加第三条注释。

#### sim-campaign 那条红是**噪声,不是问题**——闸门本身已经修掉

上一版这里写着「第 2 章曲线太平:前半均 48% vs 后半均 46%,**先修这个再做别的**,
修法是重搜 `deckTier`」。**别照做** —— 照做会把噪声焊进难度曲线。

实测:同一份代码、同一份卡池,`GAMES=60` 红,`GAMES=240` 绿
(第二章前半 52% vs 后半 40%,落差 12.3,要求 ≥8)。那 2 个点的差,
在 60 局下的差值标准误是 ±4.5pp —— 什么结论都下不了。
`campaign.ts` 顶部其实早就记着这件事(「2 个点的差被当成结论,差点据此去调关卡」),
但当时**只把结论写进了注释,脚本没改**,于是它又咬了一次。

这一轮把闸门本身修了:

- 三条判定从「点估计 vs 写死阈值」换成**显著性检验**(z>2 才算越界),z 一并打印
- 默认局数 60 → 240。60 局下「章内落差 ≥8」那一条**即使真实落差为 0 也只有 z=1.8**,
  也就是根本不可能红 —— 一道既会误报、又抓不到真问题的闸门比没有更糟
- 样本撑不起判定时脚本会自己说出来(`ℹ 第 N 章:落差判定在 M 局下没有分辨力`),
  不让「测不动的绿」被读成「没问题」
- 判定逻辑抽到 `scripts/campaignGate.ts`,配 `campaignGate.test.ts`
  **两个方向都验**(全平/倒挂/开章劝退/末关太软必须红;实测数据必须绿),
  几毫秒跑完,不用等十分钟的模拟。`vitest.config.ts` 因此纳入了 `scripts/**/*.test.ts`
- `.github/workflows/ci.yml` 去掉了 `GAMES: 60` 的覆盖(它正是 main 上那次红的直接原因),
  balance job 超时 20 → 45 分钟

**代价说清楚:CI 的 balance job 会慢约 9 分钟**(本机 192s → 727s)。
这是买「闸门的结论算数」必须付的钱;要省,只能省在降低判定要求上,那等于没有闸门。

#### sim-hero-mirror 曾经 4 红 —— 是**尺子坏了**,现已修好

这道闸门此前报「6 个备选主公 4 个出界」(劉秀 32% · 張遼 31% · 呂蒙 30% · 莊周 24%),
而 `heroes.ts` 据此劝退过两轮设计尝试。**那些数字全部作废。**

根因:`sim-hero-mirror` 把座位和先后手绑死了。

```ts
const altSeat = i & 1              // 0,1,0,1,…
const seed    = i * 131 + 7        // 131 是奇数 → seed 奇偶随 i 翻转
first: (seed & 1)                  // 于是 first 恒等于 1 - altSeat
```

注释写着「轮流坐先后手」,实际只轮换了**座位** ——
**备选主公 400 局全程后手,一局先手都没坐过**。

而这游戏的先手优势极大。自我对镜(同一个主公两边、同一副牌、同一个 AI,
唯一的不对称只剩谁先手)实测:

```
劉備 29.0%  曹操 24.5%  孔子 25.0%  司馬懿 28.0%  孫權 25.0%  老子 27.0%
                              ↑ 理论值必须是 50%
解耦先后手后:48.5 / 51.0 / 50.5 / 52.5 / 53.5 / 53.0   ✓
```

起手牌已经补过了(先手 3 张、后手 4 张),那一张补不回这 20 多个点。
**也就是说这道闸门的中性点是约 26%,而它一直拿 40–60% 判定。**

修好后重测(400 局,±2.5),结论整个翻过来:

| 备选 | 旧(坏尺子) | 新(修正后) | |
|---|---|---|---|
| 劉秀 | 33.3% | 54.5% | ✓ |
| 張遼 | 32.3% | 52.5% | ✓ |
| 郭嘉 | 40.0% | 57.8% | ✓ |
| 呂蒙 | 32.3% | 57.8% | ✓ |
| 莊周 | 25.0% | 53.3% | ✓ |
| **朱熹** | 50.5%(当年唯一「合格」) | **70.8%** | ❌ |

被反复抢救的呂蒙、劉秀**从来没有偏弱**;真正出界的是当年看起来最健康的那个。
朱熹被「太弱」的假象连加了两次码(增益→2 伤→2 伤+护甲),而那两次的依据
(31% / 37%)换算回来本来就是 53% / 59%,都在带里。

已按实测回退一半:四个候选各 400 局 ——
2伤+护甲 70.8% · 2伤 65.3%(光去护甲不够) · **1伤+护甲 46.5%** · 1伤 45.5%。
取「1 伤 + 1 护甲」,数值回到带中央,护甲的风味和与其他三家纯点杀的区分度都保住。
(顺带印证铁律 8:护甲几乎不值钱 —— 46.5 vs 45.5 的差在噪声里。)

**同一个 bug 在 `sim-balance.ts` 里发生过、修过**,那个文件顶部还留着警告
「first 必须显式传入,不能从 seed 推」—— 但没有传到隔壁。
所以这次把不变量钉成了断言:`scripts/simSeating.test.ts`,
含一条**反向验证**(复现旧写法,断言它确实会让被测方 0 局先手)。
座位编排抽到 `scripts/simSeating.ts` 共用。
`sim-balance` 没动 —— 它本来就是对的,而改它要重验整张预组矩阵,不值当。

CI 里那个 `GAMES: 100` 覆盖也一并去掉了,用脚本默认的 400
(100 局 SE 5pp 撑不起 ±10pp 的判定,这正是当年把默认值提到 400 的原因)。

### 🔴 先手优势 73.8% —— 现有闸门一道都看不见它

2026-08-04 修 sim-hero-mirror 时顺手量到的,**可能是这个游戏当下最大的平衡问题**。

六套预组自我对镜(同一套牌打自己 —— 双方卡组、主公、AI 全同,
唯一的不对称是谁先手),400 局/套,合计 2400 局:

```
桃園仁德 70.5   魏武揮鞭 74.0   克己復禮 78.0
鷹視狼顧 74.0   坐斷東南 73.3   大隱於市 73.0
--------------------------------------------
合计 73.8% ±1.0   [z=20.9]
```

**先手方赢的概率是后手方的 2.82 倍。** 六套之间高度一致(70.5–78.0),
所以这不是某套牌的毛病,是游戏本身的。

后手补偿其实已经有了 —— 起手牌先手 3 张、后手 4 张(`engine/init` 的 `OPENING_HAND`)——
那一张显然补不回二十多个点。

**这不是贪心 AI 的假象**(`AI=tiers npm run sim-firstplayer`,每档 1440 局,±1.3):

```
recruit  66.9%  +16.9      ← 失误率 35%
veteran  69.9%  +19.9      ← 失误率 12%
normal   73.9%  +23.9      ← 其它闸门用的基准尺
general  69.4%  +19.4      ← 零失误 + 前瞻 + smartMulligan
marshal  74.4%  +24.4      ← 再加整回合规划
```

这一跑没有推翻结论,但推翻了我原来的**理由**:我以为「AI 越强 → 先手优势越小」,
实际**不单调** —— 只有 `general` 掉下来(−4.5),`marshal` 又弹回最高。两条解释并存:

- `general` 比 `normal` 多的是**前瞻 + smartMulligan**。调度正是后手用那多出来的
  一张牌翻盘的主要手段,所以它把优势压下去一点。
- `marshal` 在此之上多的是**整回合规划**。规划器把每回合的场面收益榨到最大,
  而先手方就是比后手多一个回合 —— 越会榨,先手越值钱。

**五种差异极大的决策方式,先手优势稳定在 +16.9 ~ +24.4,没有一档接近可接受范围。**
所以它是结构性的,不是尺子的毛病。(仍然全是 AI,真人未必如此 —— 但
「换五把差别这么大的尺子都量出同一个量级」已经是很强的证据。)

**为什么以前没人发现**:`sim-balance` 与 `sim-campaign` 的座位与先后手都是各自
独立轮换的,先手优势在那些数字里被**平均掉了**。也就是说这条偏置可以一直存在
而不触发任何东西,直到有人单独去量。`npm run sim-firstplayer` 就是干这个的。

**补偿方案试算**(`COMP=sweep npm run sim-firstplayer`,8 方案 × 6 套 × 240 局,±1.3):

```
none             73.9%   仍然不够(现状:后手多 1 张起手牌)
hand+1           63.7%   仍然不够
hand+2           57.4%   仍然不够
hand+3           50.0%   ✓  但那是起手 3 / 7,结构性改动很大
cost-1           54.4%   ✓  单个旋钮就够 —— 起手手牌各减 1 费
armor+3          72.2%   仍然不够
armor+6          69.7%   仍然不够
hand+1,armor+3   60.7%   仍然不够
```

两条读得出来的东西:

- **一张起手牌约值 10 / 6 / 7 个百分点**(递减)。要靠抽牌压到 50% 得给到 3/7。
- **护甲几乎买不到胜率**:给后手 6 点护甲只值 4.2pp,不到一张牌的一半。
  这是铁律 8「治疗与护甲在贪心 AI 评分里近乎为零」的又一次实证 ——
  也提醒一句:**这把尺子对防守向补偿系统性低估**,真人玩家那里护甲会更值钱。

不需要改引擎一行 —— `RunModifiers` 早就有这些旋钮(远征宝物在用),而它是
`GameConfig` 的一部分、按座位给。

⚠️ `cost-1` 是**近似**而非「先攻币」:它让起手每张牌都永久便宜 1 费(附魔层实现,
打出去之前一直有效),比炉石那张一次性 +1 法力的币更持久。要做真正的币,
得给后手发一张一次性的 `gainMana{temporary:true}` 锦囊 —— 那是内容层的事,
数值大概落在 `cost-1` 与 `hand+1` 之间,上线前单独量一遍。

⚠️ **但这不是一个可以顺手落地的改动。** 补偿会改变**所有卡的相对价值**
(多抽牌利好控场,临时法力利好抢节奏),而预组骨架是跨很多轮 sim-balance 手调出来的。
这正是「兵种相克为什么撤」那一节总结出来的通则:任何全局规则改动,
**要么和预组重调一起做,要么做成 opt-in**。所以次序应该是:
先定方案 → 再重跑 sim-balance 并重调预组 → 最后才把 `sim-firstplayer` 接进 CI。
**现在没接进 ci.yml** 就是这个原因:接进去只会让 CI 一直红着,
而红着的闸门很快会被当成背景噪音 —— 那比没有闸门更糟。

### 平衡闸门怎么读

两个模拟都是**确定性的**(种子固定,同一份卡池重跑逐格一致),但输出里印的置信半宽
说的是另一件事:这些局只是「所有可能对局」的一个样本。

```
sim-balance      总胜率 ±4.4(500 局) · 单格 ±9.8(100 局)
sim-campaign     每关 ±6(240 局,新默认)
sim-hero-mirror  每个备选 ±2.5(400 局)
```

**两版之间小于半宽的差值不要拿来调参。**

还有两条比半宽更狠的教训,都是这一轮踩出来的:

1. **闸门的判定要配得上它的样本量。** 光印半宽没用 —— 判定本身若是
   「点估计 vs 写死阈值」,它既会被噪声判红,又可能在真出问题时红不了
   (sim-campaign 的落差判定在 60 局下即使真实落差为 0 也只有 z=1.8)。
2. **先验证尺子,再读数。** 对镜类模拟有个免费的自检:
   **两边放同一个东西,结果必须是 50%**。sim-hero-mirror 从来没做过这一步,
   于是带着 26% 的中性点跑了很久,还劝退过两轮设计。
   任何对称的模拟都该先过这一关。

---

## 2. 接下来做什么

标记:🟢 不碰平衡闸门 · 🟡 要跑 sim-balance / sim-campaign · ⏱ 半天以上

### A. 机制(每条单独做,单独过闸门)

> **不要一次做多条。** 已经吃过两次亏:一批改动一起上,矩阵红了查不出是哪条。

1. 🟡 **粮尽惩罚** —— 屯粮(`PlayerState.supply`)现在只有正收益,没有「粮尽」的代价。
2. 🟡 **士气崩溃** —— 士气到 `-MORALE_CAP` 时应有比「全场 -1 攻」更重的后果(溃散 / 逃兵)。
3. 🟡 **将星陨落** —— 传奇武将阵亡时的一次性全场事件。需要新 `GameEvent`(见铁律 7 的三处同步)。
4. 🟡 **同袍上桌** —— `CountSource.friendlyBattle` 已经做好(142 张卡带 `battles`),
   但只落到 2 张卡上:**带战役名单的人绝大多数是签名卡,而签名卡完全手写、不参与播种**。
   要让它真正上桌,得手写几张吃它的卡(锦囊或签名卡覆盖),这是内容设计不是播种能解决的。
5. 🟡 **家族「夷三族」** —— 结局字段里有 168 人是被诛的,现成的负向叙事,还没接机制。
6. 🟡 **降将机制** —— `LORE.defected`(65 人)已抽好,只做展示。
7. 🟡 **师承机制化** —— 关系网里 38 条 `mentor`,只做展示。
8. 🟡 **阵形补四种** —— 现有锋矢/鹤翼/鱼鳞/长蛇,八阵图还差一半。
9. 🟡 **高费段扩充** —— 9–10 费只有 70 张,一局最多出一张的牌本该是记忆点。
10. 🟡 **锦囊播种** —— 播种从不产锦囊,锦囊全是手写的。
11. 🟡 **四条冷门 op 补卡** —— `lint-content` 报 `addToHand` / `banish` / `shuffleIntoDeck` /
    `transform` 各只有 1–2 张卡在用。

### B. 模式与系统

12. 🟡 **难度选项**(简单 / 标准 / 史实)—— 默认档不变则 `sim-campaign` 照测标准档,风险最低。
13. 🟡⏱ **战役模式** —— 战役索引(24 场 / 150 人次)已经在库里,
    「一场战役 = 一局,双方阵容从索引取」是现成的。
14. 🟡⏱ **冒险第四章** —— 现为三章 24 关。
15. 🟢 **每日挑战多样化** —— 现在只有斩杀谜题。
16. 🟢 **牌组模板库** —— 按羁绊 / 家族 / 兵种给二十套。
17. 🟢 **成就隐藏档** —— 用真史料条件(「用关羽单挑击败颜良」)。
18. 🟢 **新手前三十分钟串线** —— 讲堂 / 实练 / 预组都有,没串起来。

### C. 内容(手写,零平衡风险)

19. 🟢 **名言** 253 → 300+(史诗档还缺 155)。
20. 🟢 **出战台词** 307 → 400+(源头 `officerLines` 只有 taunt/ult 两类各 147 人,已挖尽,只能手写)。
21. 🟢 **兵器坐骑** 19 → 60+(**源头生平里具名兵器一条都没有**,实测过,只能手写)。
22. 🟢 **宿敌** 41 → 60(双方同吃,实测加了 12 条后矩阵逐格不变,是最安全的一类内容)。
23. 🟡 **羁绊** 31 → 50(**只增益一侧**,必须跑闸门)。
24. 🟢 **卡面风味句** —— 大量白板卡一句话都没有。
25. 🟢 **冒险关卡过场文** —— 24 关现在只有 intro。

**手写史料的两条标准**(见 `src/content/overrides/lore-quotes.ts` 文件头):
- `quote` 名言 —— **必须有确切出处**,注释里标明。拿不准宁可空着。
  伪托同样不算(石達開《入川題壁》就是因此没写)。
- `line` 台词 / `arms` 兵器 —— 是游戏风味或演义,可以写,但注释里要标明出自演义还是正史。

### D. 界面

26. 🟢 关系图谱按 kind 筛选
27. 🟢 家族树可视化(现在是名单)
28. 🟢 时代长卷标出战役
29. 🟢 收藏进度按新档案字段(「有名言的收集了几个」)
30. 🟢⏱ 暗色 / 亮色主题
31. 🟢 色盲模式(主义现在靠颜色分)
32. 🟢 字号档位
33. 🟢⏱ 横屏平板布局
34. 🟢 卡面绰号(37 张;卡面是小尺寸三行布局,**改之前要截图验证**)

### E. 工程

35. 🟢 卡池快照测试(防无意改动)
36. 🟢 CSS 死样式检测(这一轮手工删过两批)
37. 🟢 ~~闸门自检推广~~ —— **2026-08-04 做完了大半**。现在五份纯函数判定层各带自检:
    `campaignGate`(该红时红)· `simSeating`(先后手不许和座位绑死,含反向验证)·
    `firstPlayerGate`· `balanceGate`(含「猜拳局面」)· `baseline`(分得清噪声)。
    做法可以照抄:把判定从「跑模拟的脚本」里抽成纯函数,再喂合成数据两个方向各验一遍,
    几毫秒跑完。**还没抽的**:`perf-budget` · `replay-diff` · `lint-content`。
    另注:`balanceGate` 的文件头算清了「阈值配不配得上样本量」——
    结论是**配得上,不要把它也统一成 z 检验**,那只会让平衡闸门更难红。
38. 🟡 **坐斷東南** 是六套里最低的一套,长期贴着 40% 下沿
39. 🟡 **冒险第三章方差大** —— 于謙 2% vs 虞允文 60%,同章内差 58 个点
40. 🟡 **`sim-cards` 扫过 400 张了(2026-08-04),抓到三张真的超模** ——
    先读下面那段「怎么读 sim-cards」再动手。

    | 卡 | 60 局 | 600 局复测 | price-cards 有没有抓到 |
    |---|---|---|---|
    | 姜維(6 费 5/6 铁壁+冲锋,传奇) | +28.3 | **+29.2** | ✅ 卡面 25.1 ≈10 费(+4) |
    | 簡雍(2 费 2/1,战吼弹回一名敌将) | +16.7 | **+17.0** | ❌ 没上榜 |
    | 蘇飛(3 费 3/2,战吼全体敌将 2 点) | +20.0 | **+14.0** | ❌ 没上榜 |

    **姜維 两个工具都指认**,置信度最高;`+29.2` 是把 2 张换进桃園仁德后
    整套牌的胜率变化,这个量级已经不是「偏强」而是坏掉了。

    **2026-08-05 扫了 600 张(六个主义全覆盖),又确认六张**,全部 600 局复测过:

    | 卡 | 费 | 效果 | Δ | z | price-cards 怎么看 |
    |---|---|---|---|---|---|
    | 華雄 | 5 | 5/3 單挑,战吼本回合 +3/+0 | **+22.2** | 7.7 | **正好在曲线上** |
    | 曹安民 | 1 | 1/1,战吼偷一张牌 | +17.8 | 6.2 | +1 费 |
    | 候時而動 | 3 | 抉择:正午全体 3 点 / 抽 1+粮 2 | +17.7 | 6.1 | **正好在曲线上** |
    | 項伯 | 3 | 2/1 突襲,战吼全体敌将 2 点 | +16.7 | 5.8 | **正好在曲线上** |
    | 鄭厲公 | 3 | 3/2,战吼弹回一名敌将 | +15.5 | 5.4 | **正好在曲线上** |
    | 賈復 | 7 | 6/5 突襲,战吼徵召 1 | +15.3 | 5.3 | +1 费 |

    **两组独立的同形状效果各自落在同一区间**,这不是巧合:

    ```
    弹回一名敌将   簡雍(2 费) +17.0   ·  鄭厲公(3 费) +15.5
    全体敌将 2 点   蘇飛(3 费) +14.0   ·  項伯(3 费)  +16.7
    ```

    **结论:定价公式系统性低估节奏型战吼** —— 弹回手牌、群体点杀这类
    「不产生卡差但抢走对手一整个回合」的效果。八张确认超模的卡里,
    定价表只认为三张略高(各 +1 费),另外五张(**含最强的華雄 +22.2**)
    它评为**完全在曲线上**。

    ### 偏差已经按 op 量出来了(1200 张 / 60 局,`sim-cards` 的归组汇总)

    `price-cards` 的 `opValue` 至今是一张**拍出来的点数表** —— 注释写着
    「数值取自同类卡的实际定价」,也就是**从现状反推现状**:卡池里一整类效果
    定价偏低的话,这张表会忠实地把偏差学过来,再拿它去给新卡定价。
    按 op 归组打破了这个循环 —— 它用的是**对局结果**,不是卡池自身。

    | 效果 | 张数 | 平均 Δ | z | |
    |---|---|---|---|---|
    | `returnToHand` 弹回手牌 | 25 | **+10.1** | 6.3 | 偏低 |
    | `aoeDamage` 群体伤害 | 16 | **+9.4** | 4.6 | 偏低 |
    | `recruit` 徵召 | 7 | +8.8 | 2.9 | 偏低 |
    | `delay` 伏笔 | 19 | +4.3 | 2.1 | 偏低 |
    | `damage` 点杀 | **129** | +4.1 | 5.3 | 偏低 |
    | `tutor` 搜牌 | 52 | +3.5 | 2.8 | 偏低 |
    | `swapStats` 攻守互换 | 14 | **−5.8** | −3.1 | **偏高** |

    `gainArmor`(112 张,−0.3)· `heal`(63,−1.7)· `summon`(62,−1.1)·
    `draw`(104,+1.3)· `buffStats`(93,+1.3) 全部落在噪声里 —— **这些定价是准的**。

    模式很干净:**抢节奏与解场的效果被低估,加身材/回血/护甲/铺场的没有。**
    这正好解释了为什么八张确认超模的卡里五张被评为「在曲线上」。

    两个混淆做过对照,别再担心:
    - 全池 1200 张平均 Δ 只有 **+0.58** —— 不存在「有效果的卡整体强于白板」的抬升
    - z 相对**全池均值**算,不是相对 0 —— 基准只测一次,是所有 Δ 的共用常数项

    ⚠️ **别把「平均 Δ」直接当新分值。** 它量的是「带这个 op 的卡整体偏强多少」,
    不是「这个 op 本身值多少分」—— 一张卡常带好几个 op,归组时在每组各算一次。
    拿它定**方向与量级**,改完再扫一遍看 `price-cards` 的排序是否更贴近实测。
    **这是一次真正的校准,不是又一次拍脑袋。**

    这几张各自单独改、各自跑一轮闸门(不要一批一起上,教训见 A 节开头)。

    **2026-08-05:可测范围从 51.3% 提到 100%。** 基准从前写死桃園仁德(王道),
    而待测卡的主义必须与基准兼容 —— 于是霸道 294 / 名利 335 / 割据 210 /
    礼教 196 / 隐逸 142 共 **1,177 张专属卡一张都没量过**。
    现在按主义挑对应预组当基准,Δ 相对它自己那套算。
    中立卡仍固定用桃園仁德(保持与历史数字可比,且中立卡进哪套都合法)。

    ⚠️ **怎么读 sim-cards**:同一跑里榜首往下的那一长串**大部分是噪声**。
    400 张 / 60 局那次报了「偏离超过 ±4 的 235 张」,而纯随机下就该有约 216 张 ——
    两者几乎无法区分。原因是 Δ 是两次测量之差(60 局时标准误 ±9.1),
    而且做了 400 次比较。脚本现在按族错误率给校正线,并分两档报。
    **但「没越过校正线」不等于「里面没东西」**:上面那三张正是从没越线的榜首里
    复测出来的,而负向的三张(岳飛 −16.7 → −5.7、杜襲 −16.7 → −5.8、
    王離 −18.3 → −7.7)整齐地塌回去了 —— 典型的均值回归。
    **正确用法是两段式**:小样本扫全池排序 → 榜首拿去 600 局复测 → 复测后仍然大的才当真。

---

## 3. 已经查实做不了的(**别再试**)

这些都量过,写在这里是为了不让下一个人重做一遍:

| 项 | 结论 |
|---|---|
| 表字覆盖率 | **53.3% 是天花板**。传里出现「字」的 877 人只漏 7 个,那 7 个确实没有表字。 |
| 生卒年 | **50.6% 是天花板**。从生平再抠只多 7 条。 |
| 出战台词 | 源头只有 taunt/ult 两类各 147 人,**已挖尽**。 |
| 具名兵器 | 源头生平里 **0 条**(泛指的「弩」「矛」只有 18 条)。坐骑 4、身高 8、容貌 8。 |
| 生平简→繁转换 | **不能做**。48 个简体字里只有「内」一个是繁體中不用的,其余(征 后 面 于 云 岳 范 巨 松…)本身就是合法繁體字 —— 转了会把「征荊州」变成「徵荊州」、「岳飛」变成「嶽飛」。 |
| 繁简折叠对事迹抽取 | 实测只多 **4 次**命中,收益全在补词表。别把功劳记在它头上。 |
| 1 费重复度 | 70% 有双胞胎,**降不下去** —— 1 费只有 3 点身材预算,买不起第二样东西。唯一的路是 0.3 点的免费微效果(`payFor` 的扣款门槛是 0.4)。 |
| 举荐 / 子嗣 / 出仕年龄 / 谥号 | 分别只有 22 / 22 / 3 / **0** 条,撑不起一栏。 |
| **兵种相克** | **做完了,测出来不行,撤了。** 见下。 |

### 兵种相克为什么撤

骑→弓→步→骑,攻击方 +1 伤害。引擎、测试、文案全写完,闸门不过:

```
坐斷東南       43.6% → 39.0%   掉出 40–60%
魏武揮鞭 vs 坐斷東南   71%      冲破 30–70%
```

原因是结构性的 —— 六套预组的兵种构成是**这条规则出现之前**定死的:

```
魏武揮鞭  騎11 步8 謀2 弓1     ← 步8 打进对面骑9,吃满加成
坐斷東南  騎9 謀6 步5 弓2      ← 步只有 5,反过来吃不回来
```

这条规则不改任何一张卡,却白送了魏约 4.6 个百分点。而预组骨架是跨很多轮
`sim-balance` 手调出来的,极其敏感(颜真卿 4/6 → 4/4 一张牌就能把矩阵打出闸门)。

**推广到一条通则**:任何**全局战斗规则**(相克、地形、天气减伤)都有这个性质 ——
它不改任何一张卡,却改变所有卡的相对价值,而相对价值正是预组调了很多轮的东西。
这类改动**要么和预组重调一起做,要么做成 opt-in**(挂 `FieldRule` 或乱斗规则,
双方同吃且不进天梯与关底)。

---

## 4. 这个仓库特有的坑(踩过的)

1. **改引擎类型跑 `npm run build`,不是 `tsc --noEmit`** ——
   后者只走根 tsconfig,test/server/drivetest 三份配置是 build 才检的。
   vitest 不做类型检查,所以会出现「982 个单测全绿、只有构建红」。

2. **静默失效是这个仓库最贵的一类 bug。** 它们不崩不红,只是「有东西不见了」。
   已知的几类各有一道源码级闸门(见 `src/**/*.test.ts` 里带「闸门」字样的注释):
   - 新 `GameEvent` 要补 eventText / floats / useEventAnimations 三处
   - 新 `PlayerState` / `GameState` 字段要补裁剪层两处(否则**单机全对、联机全无**)
   - 新关键词要补讲堂词条
   - `Record<string, T>` 覆盖表**撞键 tsc 不报错**,后写的整条盖掉先写的
   - 显示层直接读生成层会**看不见手写补遗**(列传屏就这么漏过几轮)

3. **覆盖表里的手写内容必须是原文里的原话。** `src/content/dossier.test.ts` 会去断言
   籍贯 / 结局 / 绰号 / 镇守 / 归附 都能在那个人的生平里原样找到。照抄可以,推断不行。

4. **先量再写。** 凭印象排出来的清单有一半是空的 —— 上一轮八项估计错了五项,
   而且都是高估(估 200 实测 10、估 40 实测 0)。写抽取规则之前先写十行探针量一遍。

5. **偶发红先怀疑数据不一致,别当 flake 放过** ——
   `arena.test` 三次红一次那回,根因是两张卡撞了 id。
   但确定性种子的长测试(fuzz / replay)单独重跑能过,那就是并行跑重活时的超时。

6. **这个仓库可能有另一个会话同时在改。** 提交用
   `git commit -F - -- <明确路径>` 而不是 `git add -A` ——
   前者只提交列出路径的工作区状态,和索引里有什么无关,免疫竞态。

7. **把教训写进注释 ≠ 修好了。** 2026-08-04 一天之内撞见三次同一个模式:
   有人发现了问题、把结论工工整整写进注释、然后代码照旧。
   - `sim-campaign` 的「60 局撑不起自己的判定」写在 `campaign.ts` 顶部 ——
     脚本没改,默认还是 60,CI 还钉着 `GAMES: 60`,于是 main 上挂着一次假红,
     而 ROADMAP 把那次误报当成了待办第一条。
   - `sim-hero-mirror` 的「first 必须显式传入,不能从 seed 推」写在 `sim-balance.ts` 顶部 ——
     隔壁文件一字不差又写了一遍那个 bug,还带着歪尺子劝退了两轮主公技设计。
   - `price-cards` 的 `opValue` 没有 default,新 op 加进引擎时没人回头补表。
   **修法一律是可执行的断言,不是第三条注释。** 判定逻辑抽成纯函数 + 合成数据双向验证,
   几毫秒跑完(样板见 `scripts/campaignGate.test.ts`)。

8. **先验证尺子,再读数。** 对称的模拟有个免费且极强的自检:
   **两边放同一个东西,结果必须是 50%**。
   `sim-hero-mirror` 从来没做过这一步,于是带着 26% 的中性点跑了很久 ——
   六个备选主公里四个被误判「过弱」,而唯一「合格」的那个才是真正出界的。
   任何对镜/镜像类模拟上线前先跑这一关(`npm run sim-firstplayer` 顺带就是它)。

---

## 5. 常用命令

```bash
# 闸门(有明确 exit code,CI 在跑)
npm run build            # 类型检查 + 构建
npm test                 # 单测
npm run lint-content     # 卡池结构性自检
npm run perf-budget      # 首屏体积(总量 + 按 chunk 基线)
npm run replay-diff      # 引擎确定性对拍
npm run check-offline    # PWA 断网可玩
npm run check-generated  # 生成层漂移(没有素材源时自动跳过)

# 环境(几秒,先跑这个)
npm run doctor           # 这台机器上什么能跑、什么不能、为什么(附闸门耗时表)

# 平衡(**下结论一律用默认局数或更高**)
#
# 五道全部并行(worker 池,见 scripts/parallel.ts),耗时为 10 线程实测。
# 跑一遍全套:**11 分钟**(串行要 39 分钟)。
npm run sim-campaign     # 冒险 24 关难度曲线            ~4min   (串行 12min)
npm run sim-firstplayer  # 先手优势 + 对镜类模拟的仪器自检 ~2.5min (串行 5min)
                         #   AI=tiers 换尺子 · COMP=sweep 试算后手补偿
npm run sim-cards        # 单卡边际胜率(SAMPLE=400 可扫一大片)
npm run sim-hero-mirror  # 备选主公技对镜                ~1.7min (串行 8.5min)
npm run sim-balance      # 六套预组互搏矩阵              ~1min   (串行 5min)
                         #   跑完自动与上次基线对比
npm run tune-campaign    # 关底 deckTier 网格扫描(只打印建议,不改代码)

#   JOBS=1 退回单线程 —— 怀疑并行接错时用它对拍,走的是同一份代码
#   JOBS=N 指定线程数;默认 核数-2,最多 16
#
# **并行不影响任何一个数字**:每局的种子只由索引决定、引擎是纯函数,
# 所以按索引装回去就与串行逐位一致。三道接入时都做过并行/串行对拍。

# 盘点(不卡门,只记录)
npm run audit-generals   # 武将档案覆盖率 + 机制重复度
npm run audit-en         # 英文漏翻
npm run audit-zh         # 繁简覆盖

# 内容重新生成(**需要 ../ThreeKingdomMastersIOS**)
npm run import-content
```
