# 待办与交接

> 写给**在另一台机器上克隆本仓库后接手的人**(含 AI 助手)。
> 动手前请先完整读 [ARCHITECTURE.md](ARCHITECTURE.md) —— 那里是工程约定与架构铁律,
> 本文只写「接下来做什么、以及为什么不做某些事」。
>
> 最后更新:2026-08-06

---

## 0. 新机器上先做这四件事

```bash
npm ci
npm run build          # 类型检查 + 构建(4 份 tsconfig 一起过)
npm test               # 单元测试(含 server/)
npm run lint-content   # 卡池结构性自检
```

四条都绿,说明环境是好的。**不需要克隆任何别的仓库**:
`src/` 一行都不 import 外部素材源,生成产物(`src/content/generated/`)已入库。

> **Windows 上跑得通了**(2026-08-04)。此前 `npm test` 在 Windows 必红三条,
> 报 `ENOENT ... 'C:\C:\...\All%20Codes\...'` —— 三个扫源码的测试用
> `new URL('.', import.meta.url).pathname` 取目录,而它在 Windows 上给的是
> `/C:/…/All%20Codes/…`:多一个前导斜杠、空格还是 `%20`。macOS 上照跑,
> 所以这一类**只在换机器时才炸**,而这份文档正是写给换机器的人的。
> 已统一改成 `dirname(fileURLToPath(import.meta.url))`(仓库里 `purity.test`
> 等一直是这种写法)。注意 `readFileSync(new URL(...))` 传 URL **对象**是安全的,
> Node 自己会解码 —— 坏的只有 `.pathname` 那个字符串。

### 唯一的例外:素材源头

`npm run import-content` / `check-generated` / `export-portraits` 依赖同级目录的
`../ThreeKingdomMastersIOS`(另一个项目,只读的素材源)。**这台机器上没有它是正常的** ——
那三条命令会自己跳过或报找不到,其余一切照常。

**没有它就不要动 `scripts/import-content.ts` 与 `scripts/seed-mechanics.ts`** ——
改了也没法重新生成,更没法验证。这两个文件的改动必须在有素材源的机器上做。

---

## 1. 当前状态(接手前必读)

| | 数值 |
|---|---|
| 卡池 | 2,471 张(可收集 2,451) |
| 测试 | 1384 通过 / 137 个文件(**九份**闸门自检 + **七份**清单类判定层 + 卡池快照 + 覆盖层冲突) |
| 首屏 | 余量 36.8 KB(主包 166.2/190 · 内容层 128.7/150 · 第三方 60.2/75) |
| 羁绊 / 宿敌 | 52 条 / 60 对 |

### 2026-08-08 这一轮:A 组(机制)做完了,只剩一条卡在素材上

**A1 粮尽 · A2 溃散 · A3 将星陨落 · A4 同袍 · A5 夷三族 · A6 降将 · A7 师承 · A8 八阵 · A9 高费段**
全部落地,各自跑过 `sim-balance` + `sim-campaign`;**A10 锦囊播种 ⛔**(要改 `seed-mechanics.ts`,
这台机器上没有姊妹仓库)。新增第二十六至三十一共六个卡包。

**这一轮量出来的、以后能直接用的四条:**

1. **阵形的强度几乎全写在门槛上,不在增益数值上。** 一个字没改增益、只把门槛
   从 4 降到 3,三张从 −7.3 / −3.0 / −10.5 变成 −1.2 / −1.2 / −6.8。
2. **部族 payoff:印在卡面上的身材要按同费白板给足,部族那一层是纯上不封顶。**
   把部族收益预扣进身材,在 0 层的牌组里必然塌成死牌(降将三张 −13.8 / −7.8 / −3.0)。
3. **九费不是「反正打不出来」。** 我写过一段「顶费卡天生难看」的预判,
   第一轮实测 焚舟 **+27.3**。「这一档天生难看」这种话必须先量再说。
4. **单侧清场是形状问题,不是代价问题。** 焚舟 消滅全场 +27.3 → 单侧 5 点 +18.7 →
   **双方**5 点 +1.3。贵到几费,无条件还是无条件。

**顺带补上的三个洞(都是「闸门自己瞎了」那一类):**
- `bossDeck` 的打分函数**不认识 `supplyCost`** —— 军需卡在它眼里是白送的身材,
  选进 Boss 卡组之后付不出粮一直躺在手里(岳飛 47.5% → 63.7%,闸门 exit 1)。
- 卡池快照**看不见部族标签** —— 贴了 65 张 `defector`,diff 只有 3 行。
- **流程教训:内容包也得跑 `sim-campaign`。** `sim-balance` 看的是六套手写预组
  (加新卡不动它),`sim-campaign` 看的是从**全池**确定性挑的 Boss 卡组(加新卡会动)。
  详见第 4 节坑 12。

### ✅ 闸门的现状(2026-08-09 更新)

**全绿** —— `sim-balance`(六套 46.9–53.8%)· `sim-campaign`(四章曲线合理)·
`sim-history`(17 场 47–64%)· `deck-stats`(骨架无独占超限)·
单测 1453 / 144 文件 · `build` · `eslint` · `typecheck` ·
`css-order` · `dead-css` · `lint-content` · `audit-en` · `perf-budget`。

⚠️ **这份名单本身是这一轮的产出之一。** 上一版名单里**没有 `sim-history`** ——
而它红了很久都没人看见,正是因为不在名单上。
**一道闸门不在清单上,等于没有。** 加新闸门时,除了写测试,记得把它加进这一行。

---

以下是 2026-08-07 的记录:

**全绿,而且是在一次全局规则改动之后全绿的** —— `sim-balance`(六套 45.2–57.8%)·
`sim-campaign`(三章曲线合理)· `sim-hero-mirror`(六个 46.0–57.8%)·
**`sim-firstplayer`(51.2%,这一天才第一次有资格当闸门)** ·
`replay-diff` · `perf-budget` · `css-order` · `check-offline` · `lint-content` · `check-generated`。

那次改动是**后手补偿**(先手优势 73.8% → 51.2%,见下),它按预期连带推动了
关底战曲线与备选主公技两道闸门,两处都跟着重调了 —— 这正是「任何全局规则改动
要么和预组重调一起做、要么做成 opt-in」那条通则的一次完整执行。

---

以下是 2026-08-04 那一轮的记录,**结论仍然成立**:那一轮真正的产出不是「把红变绿」,
而是**两道闸门自己是坏的**:

- **`npm run sim-balance` 绿** —— 六套预组总胜率全部落在 40–60%(坐斷東南 42.6% 最低)。
- **`npm run sim-campaign` 绿** —— 此前那条红是**噪声**,判定撑不起自己的样本量。见下。
- **`npm run sim-hero-mirror` 绿** —— 此前那 4 条红是**尺子坏了**(备选主公全程后手),
  修好后只剩朱熹一个真出界,已按实测调回。见下。

另外两道**不在上面三道之列、但同样在 main 上红着或失灵**的,一并修了:

- **`npm run check-offline` 在 main 上是红的** —— 报 5 项假缺失。
  根因是底图那次 JPG → WebP 迁移只做了一半(CSS 与 vite.config 改了,
  而 import-content 的拷贝表、ARCHITECTURE、check-offline 三处都没改)。
  假缺失和真缺失混在一起,这道闸门实际上已经不能用了。
- **`npm run perf-budget` 的 chunk 基线能默默失效** —— 正则失配时只 `console.warn`,
  打包分块一改名它就不再守任何东西,而 CI 照样绿。已改成硬失败。

### 五道平衡模拟全部并行化(2026-08-05)

跑一遍全套 **39 分钟 → 11 分钟**。公共外壳在 `scripts/parallel.ts`(保序 worker 池)。

```
sim-balance      289s → 52s   5.5x
sim-hero-mirror  510s → 99s   5.1x
sim-cards        481s → 131s  3.7x
sim-campaign     725s → 250s  2.9x
sim-firstplayer  316s → 141s  2.25x
```

**并行不影响任何一个数字**:每局的种子只由索引决定、引擎是纯函数、
局与局之间零共享状态(铁律 1),所以按索引装回去就与串行逐位一致。
每一道接入时都做过**并行/串行逐格对拍**,`JOBS=1` 保留作单线程对拍开关。

**切分粒度要够碎**,同一个教训踩了两次:任务少而不齐时,尾部一堆线程在等最慢的那个
(sim-campaign 按关切只有 2.0x → 按局段切才 2.9x;sim-firstplayer CHUNK=60 只有 1.9x
→ CHUNK=20 才拉开)。只有 `sim-cards` 天然等长,不必再切。

意义不在「快了几倍」,而在**样本量不再需要权衡** —— 这一轮踩的坑一大半根子都是
「样本量不够又懒得加」,现在 `GAMES=1000` 也就几分钟。

---

一句话总结这一轮:**红的那几道没有一道是内容的问题,全是量它们的工具的问题。**
而根因高度一致 ——「有人发现过、写进注释、然后就没有然后了」,
或者「一次迁移只改了一半,剩下的地方悄悄失灵」。
所以修法一律是**把不变量钉成可执行断言**,不再加第三条注释。

#### sim-campaign 那条红是**噪声,不是问题**——闸门本身已经修掉

上一版这里写着「第 2 章曲线太平:前半均 48% vs 后半均 46%,**先修这个再做别的**,
修法是重搜 `deckTier`」。**别照做** —— 照做会把噪声焊进难度曲线。

实测:同一份代码、同一份卡池,`GAMES=60` 红,`GAMES=240` 绿
(第二章前半 52% vs 后半 40%,落差 12.3,要求 ≥8)。那 2 个点的差,
在 60 局下的差值标准误是 ±4.5pp —— 什么结论都下不了。
`campaign.ts` 顶部其实早就记着这件事(「2 个点的差被当成结论,差点据此去调关卡」),
但当时**只把结论写进了注释,脚本没改**,于是它又咬了一次。

这一轮把闸门本身修了:

- 三条判定从「点估计 vs 写死阈值」换成**显著性检验**(z>2 才算越界),z 一并打印
- 默认局数 60 → 240。60 局下「章内落差 ≥8」那一条**即使真实落差为 0 也只有 z=1.8**,
  也就是根本不可能红 —— 一道既会误报、又抓不到真问题的闸门比没有更糟
- 样本撑不起判定时脚本会自己说出来(`ℹ 第 N 章:落差判定在 M 局下没有分辨力`),
  不让「测不动的绿」被读成「没问题」
- 判定逻辑抽到 `scripts/campaignGate.ts`,配 `campaignGate.test.ts`
  **两个方向都验**(全平/倒挂/开章劝退/末关太软必须红;实测数据必须绿),
  几毫秒跑完,不用等十分钟的模拟。`vitest.config.ts` 因此纳入了 `scripts/**/*.test.ts`
- `.github/workflows/ci.yml` 去掉了 `GAMES: 60` 的覆盖(它正是 main 上那次红的直接原因),
  balance job 超时 20 → 45 分钟

**代价说清楚:CI 的 balance job 会慢约 9 分钟**(本机 192s → 727s)。
这是买「闸门的结论算数」必须付的钱;要省,只能省在降低判定要求上,那等于没有闸门。

#### sim-hero-mirror 曾经 4 红 —— 是**尺子坏了**,现已修好

这道闸门此前报「6 个备选主公 4 个出界」(劉秀 32% · 張遼 31% · 呂蒙 30% · 莊周 24%),
而 `heroes.ts` 据此劝退过两轮设计尝试。**那些数字全部作废。**

根因:`sim-hero-mirror` 把座位和先后手绑死了。

```ts
const altSeat = i & 1              // 0,1,0,1,…
const seed    = i * 131 + 7        // 131 是奇数 → seed 奇偶随 i 翻转
first: (seed & 1)                  // 于是 first 恒等于 1 - altSeat
```

注释写着「轮流坐先后手」,实际只轮换了**座位** ——
**备选主公 400 局全程后手,一局先手都没坐过**。

而这游戏的先手优势极大。自我对镜(同一个主公两边、同一副牌、同一个 AI,
唯一的不对称只剩谁先手)实测:

```
劉備 29.0%  曹操 24.5%  孔子 25.0%  司馬懿 28.0%  孫權 25.0%  老子 27.0%
                              ↑ 理论值必须是 50%
解耦先后手后:48.5 / 51.0 / 50.5 / 52.5 / 53.5 / 53.0   ✓
```

起手牌已经补过了(先手 3 张、后手 4 张),那一张补不回这 20 多个点。
**也就是说这道闸门的中性点是约 26%,而它一直拿 40–60% 判定。**

修好后重测(400 局,±2.5),结论整个翻过来:

| 备选 | 旧(坏尺子) | 新(修正后) | |
|---|---|---|---|
| 劉秀 | 33.3% | 54.5% | ✓ |
| 張遼 | 32.3% | 52.5% | ✓ |
| 郭嘉 | 40.0% | 57.8% | ✓ |
| 呂蒙 | 32.3% | 57.8% | ✓ |
| 莊周 | 25.0% | 53.3% | ✓ |
| **朱熹** | 50.5%(当年唯一「合格」) | **70.8%** | ❌ |

被反复抢救的呂蒙、劉秀**从来没有偏弱**;真正出界的是当年看起来最健康的那个。
朱熹被「太弱」的假象连加了两次码(增益→2 伤→2 伤+护甲),而那两次的依据
(31% / 37%)换算回来本来就是 53% / 59%,都在带里。

已按实测回退一半:四个候选各 400 局 ——
2伤+护甲 70.8% · 2伤 65.3%(光去护甲不够) · **1伤+护甲 46.5%** · 1伤 45.5%。
取「1 伤 + 1 护甲」,数值回到带中央,护甲的风味和与其他三家纯点杀的区分度都保住。
(顺带印证铁律 8:护甲几乎不值钱 —— 46.5 vs 45.5 的差在噪声里。)

**同一个 bug 在 `sim-balance.ts` 里发生过、修过**,那个文件顶部还留着警告
「first 必须显式传入,不能从 seed 推」—— 但没有传到隔壁。
所以这次把不变量钉成了断言:`scripts/simSeating.test.ts`,
含一条**反向验证**(复现旧写法,断言它确实会让被测方 0 局先手)。
座位编排抽到 `scripts/simSeating.ts` 共用。
`sim-balance` 没动 —— 它本来就是对的,而改它要重验整张预组矩阵,不值当。

CI 里那个 `GAMES: 100` 覆盖也一并去掉了,用脚本默认的 400
(100 局 SE 5pp 撑不起 ±10pp 的判定,这正是当年把默认值提到 400 的原因)。

### ✅ 先手优势 73.8% → 51.2%(2026-08-07 落地)—— 现有闸门一道都看不见它

2026-08-04 修 sim-hero-mirror 时顺手量到的,**可能是这个游戏当下最大的平衡问题**。

六套预组自我对镜(同一套牌打自己 —— 双方卡组、主公、AI 全同,
唯一的不对称是谁先手),400 局/套,合计 2400 局:

```
桃園仁德 70.5   魏武揮鞭 74.0   克己復禮 78.0
鷹視狼顧 74.0   坐斷東南 73.3   大隱於市 73.0
--------------------------------------------
合计 73.8% ±1.0   [z=20.9]
```

**先手方赢的概率是后手方的 2.82 倍。** 六套之间高度一致(70.5–78.0),
所以这不是某套牌的毛病,是游戏本身的。

后手补偿其实已经有了 —— 起手牌先手 3 张、后手 4 张(`engine/init` 的 `OPENING_HAND`)——
那一张显然补不回二十多个点。

**这不是贪心 AI 的假象**(`AI=tiers npm run sim-firstplayer`,每档 1440 局,±1.3):

```
recruit  66.9%  +16.9      ← 失误率 35%
veteran  69.9%  +19.9      ← 失误率 12%
normal   73.9%  +23.9      ← 其它闸门用的基准尺
general  69.4%  +19.4      ← 零失误 + 前瞻 + smartMulligan
marshal  74.4%  +24.4      ← 再加整回合规划
```

这一跑没有推翻结论,但推翻了我原来的**理由**:我以为「AI 越强 → 先手优势越小」,
实际**不单调** —— 只有 `general` 掉下来(−4.5),`marshal` 又弹回最高。两条解释并存:

- `general` 比 `normal` 多的是**前瞻 + smartMulligan**。调度正是后手用那多出来的
  一张牌翻盘的主要手段,所以它把优势压下去一点。
- `marshal` 在此之上多的是**整回合规划**。规划器把每回合的场面收益榨到最大,
  而先手方就是比后手多一个回合 —— 越会榨,先手越值钱。

**五种差异极大的决策方式,先手优势稳定在 +16.9 ~ +24.4,没有一档接近可接受范围。**
所以它是结构性的,不是尺子的毛病。(仍然全是 AI,真人未必如此 —— 但
「换五把差别这么大的尺子都量出同一个量级」已经是很强的证据。)

**为什么以前没人发现**:`sim-balance` 与 `sim-campaign` 的座位与先后手都是各自
独立轮换的,先手优势在那些数字里被**平均掉了**。也就是说这条偏置可以一直存在
而不触发任何东西,直到有人单独去量。`npm run sim-firstplayer` 就是干这个的。

**补偿方案试算**(`COMP=sweep npm run sim-firstplayer`,**16 方案** × 6 套 × 240 局,±1.3):

```
none             73.9%   现状:后手多 1 张起手牌
hand+1           63.7%   仍然不够
hand+2           57.4%   仍然不够
hand+3           50.0%   ✓  但那是起手 3 / 7,结构性改动很大
cost-1           54.4%   ✓  单个旋钮就够 —— 起手手牌各减 1 费
armor+3          72.2%   仍然不够
armor+6          69.7%   仍然不够
hand+1,armor+3   60.7%   仍然不够
--- 2026-08-06 补测 ---
hp-1             58.7%   **主公技整局便宜 1** —— 单个旋钮里最有力的一个(−15.2pp)
hp-1,hand+1      50.8%   ✓✓ **最接近 50%,而且改动最小**
cost-1,armor+6   51.2%   ✓
hand+2,armor+6   52.2%   ✓
cost-1,armor+3   52.3%   ✓
hand+1,cost-1    41.9%   补过头了(后手反而占优)
supply+2         73.9%   ⚠ 与无补偿**逐格相同** —— 没有载体,测不了
morale+2         73.9%   ⚠ 同上
```

读得出来的东西:

- **一张起手牌约值 10 / 6 / 7 个百分点**(递减)。要靠抽牌压到 50% 得给到 3/7。
- **护甲几乎买不到胜率**:给后手 6 点护甲只值 4.2pp,不到一张牌的一半。
  这是铁律 8「治疗与护甲在贪心 AI 评分里近乎为零」的又一次实证 ——
  也提醒一句:**这把尺子对防守向补偿系统性低估**,真人玩家那里护甲会更值钱。
- 🆕 **`hp-1` 是新发现的一条大杠杆**(−15.2pp,约等于 1.5 张起手牌)。
  它和上面所有方案有个本质区别:**其余全是一次性的,只有它整局都在起作用**。
  先手的优势正来自「每一回合都早一步」,一个每回合都在补的补偿在机理上更对得上。
  `hp-1,hand+1` 落到 **50.8%**,而且改动面极小 —— 后手起手牌从 4 张变 5 张、
  主公技便宜 1。**我推荐这个。**
- ⚠️ **`supply+2` / `morale+2` 与无补偿逐格相同,那不是「没效果」** ——
  是**六套预组里一张军需卡都没有、一张吃士气的卡也没有**,加了也花不出去。
  修饰符本身接在 `engine/init` 上,没坏。脚本现在会自己逐格比对并点名,
  免得下次有人把它写成「这两条轴买不到胜率」。
  (这同时说明「粮尽惩罚」与「同袍上桌」两条待办**缺的是载体卡,不是机制**。)

不需要改引擎一行 —— `RunModifiers` 早就有这些旋钮(远征宝物在用),而它是
`GameConfig` 的一部分、按座位给。

#### 🔴 2026-08-06:`hp-1,hand+1` **真的落地试过了,平衡闸门红了,已回退**

改动只有两行:`OPENING_HAND` 从 `[3,4]` 改成 `[3,5]`,后手默认
`heroPowerCostDelta -1`(叠加在 RunModifiers 之上,不是替代)。
1129 条单测只红了 2 条,而且都是**钉住旧规则的断言**(正是它们该做的事)。

**它做到了它该做的事:**

```
先手优势  73.8%  →  51.7%   ✓ 闸门通过
```

**但 sim-balance 红了:**

```
魏武揮鞭 62.2%(超出 40–60)      坐斷東南 35.0%(超出 40–60)
魏武揮鞭 vs 坐斷東南 76%          桃園仁德 vs 坐斷東南 74%
基线对比:克己復禮 −9.6(z=3.0)· 坐斷東南 −7.6(z=2.4)· 大隱於市 +7.6(z=2.4)
```

**这正是「兵种相克」那条通则的第二次实证**(见第 3 节):
任何**全局战斗规则**都不改任何一张卡,却改变所有卡的相对价值,
而预组骨架是跨很多轮 `sim-balance` 手调出来的,极其敏感。

还有一条只有落地才看得见的东西 —— **补偿是均匀的,而先手优势不是**:

```
            改之前   改之后    降幅
鷹視狼顧     74.0    43.5    −30.5
魏武揮鞭     74.0    44.8    −29.2
桃園仁德     70.5    46.0    −24.5
克己復禮     78.0    56.5    −21.5
大隱於市     73.0    51.7    −21.3
坐斷東南     73.3    67.5     −5.8   ← 几乎没动
```

六套预组的离散度从 7.5 个百分点**涨到 24.0**。坐斷東南 吃不到这份补偿
(它本来就是六套里最弱的一套,长期贴 40% 下沿 —— 待办 43),
于是同一个改动把它又往下踩了一脚。

#### ✅ 2026-08-07:落地了 —— **73.8% → 51.2%**,五道闸门全绿

规则在 `src/engine/types.ts` 的 `SECOND_PLAYER_COMP`(默认规则,不是模式修饰符;
`RunModifiers` 叠加在它之上):**后手起手全部手牌 −1 费,并带 3 点护甲**。

**先给尺子补了一列。** 上一轮翻车的根因是「只看平均值」,所以
`COMP=sweep npm run sim-firstplayer` 现在同时报**分套离散度**,
并在「平均落在带内、但离散度是无补偿的两倍以上」时明确写「够了但不匀 —— 落地会炸 sim-balance」。
各 400 局/套实测:

```
方案              合计    离散
none             73.8%    7.5
cost-1,armor+3   51.8%   12.3   ← 选它
cost-1,armor+6   50.1%   13.0
hand+2,armor+6   52.8%   23.0
hp-1,hand+1      51.0%   23.3   ← 2026-08-06 翻车的那个
```

两个 `cost-1` 方案的离散度只有另外两个的一半,而且这**有机理**:
减起手牌费用每套预组都吃得到;主公技减费和多抽一张则挑卡组
(坐斷東南 用主公技少,所以几乎吃不到 `hp-1`)。
在两个 cost-1 里选 armor **+3 而不是 +6**:铁律 8 说这把尺子**系统性低估护甲**,
所以 AI 量到 51.8% 的方案对真人只会更接近 50,而量到 50.1% 的那个反而会补过头。

**然后按上面第 1 条走完了「连预组一起重调」。**
`cost-1` 单独也守不住闸门 —— 因为 **坐斷東南 本来就只剩 2.6pp 余量**(42.6%,
长期贴 40% 下沿,待办 43)。任何全局补偿都会把它推出界,不是补偿大小的问题。
量出来的答案很干脆:`CARDS=lu-kang GAMES=400 npm run sim-cards`
(基准正是这套牌自己)给 **陸抗 +21.5**,候选里第二名才 +12.0。
那不是「陸抗 超模」——它 5 费 3/6 在曲线上还偏低 ——
而是**这套牌缺守护缺到这个地步**:Δ 是相对所在卡组量的,它量的是缺口。

换进去两张直接冲到 60.8%(补过头),**一张**落在 48.4%。
同时把六费从八张压到七张:补偿让开局更快,顶重的一头吃亏最大。

连带修的两处(全局规则一动,别处一定会动 —— 这正是那条通则):

| 闸门 | 症状 | 处理 |
|---|---|---|
| `sim-campaign` | 第 2 章曲线太平(前半 59.5 / 后半 57.5,应差 ≥8);七个 boss 各松 +14~+32pp | 趙匡胤 / 岳飛 的 `deckTier` 0.45 → 0.90(`tune-campaign` 网格搜的,tier→强度非单调)、血量 64/66 → 65/68。后半降到 49.75,落差 9.75 ✓ |
| `sim-hero-mirror` | 呂蒙 66.0%、郭嘉 61.0% 出带 | 两个「造成 1 点伤害」的主公技 2 费 → 3 费。呂蒙 57.8%、郭嘉 50.2% ✓ |

呂蒙 那一格的病根不在它变强,而在**对照组变弱**:孫權「制衡」白送一个 0/4 守护,
而 坐斷東南 现在自带的守护够多了,那面墙变成冗余。改基准牵动的东西更多,所以从备选这一侧收。

最终:`sim-balance` 六套 45.2–57.8% ✓ · `sim-campaign` 三章曲线合理 ✓ ·
`sim-hero-mirror` 六个 46.0–57.8% ✓ · `replay-diff` 四道对拍全过 ✓ · 1266 单测 + 67 条 e2e ✓。

**玩家看得见这条规则。** 调度屏(手牌第一次露面的那一刻)给后手加一条说明:
「後手補償 · 手牌多一張、起手牌各 −1 費、主公 +3 護甲」。
截图对拍时正好抓到必须写它的理由:同一张 陳到,先手屏显示 6 费、后手屏显示 5 费 ——
不写出来玩家的第一反应是 bug 而不是规则,而**一条看不见的规则等于没有规则**。

`sim-ai-tiers` 也重跑过(全局规则一动,所有实测结论都要复验):
marshal 74.3%(改动前 71.7%),z=5.83,远在 55% 验收线之上。

规则本身现在**有测试钉着**(`src/engine/modifiers.test.ts`):
后手减费、后手护甲、**跟着先后手走而不是座位号**、以及「远征宝物叠加在补偿之上而不是替代」——
最后一条是最容易写错又不会报错的那种(`mod?.startArmor ?? COMP` 会把补偿悄悄吃掉)。

⚠️ `cost-1` 是**近似**而非「先攻币」:它让起手每张牌都永久便宜 1 费(附魔层实现,
打出去之前一直有效),比炉石那张一次性 +1 法力的币更持久。要做真正的币,
得给后手发一张一次性的 `gainMana{temporary:true}` 锦囊 —— 那是内容层的事,
数值大概落在 `cost-1` 与 `hand+1` 之间,上线前单独量一遍。

⚠️ **但这不是一个可以顺手落地的改动。** 补偿会改变**所有卡的相对价值**
(多抽牌利好控场,临时法力利好抢节奏),而预组骨架是跨很多轮 sim-balance 手调出来的。
这正是「兵种相克为什么撤」那一节总结出来的通则:任何全局规则改动,
**要么和预组重调一起做,要么做成 opt-in**。所以次序应该是:
先定方案 → 再重跑 sim-balance 并重调预组 → 最后才把 `sim-firstplayer` 接进 CI。
**2026-08-07 更新:上面这个次序真的走完了(见下),所以 `sim-firstplayer` 现在可以接进 CI 了** ——
它跑一轮 2400 局约 80 秒,和 `sim-balance` 一个量级。已接在 balance job 里。

### 平衡闸门怎么读

两个模拟都是**确定性的**(种子固定,同一份卡池重跑逐格一致),但输出里印的置信半宽
说的是另一件事:这些局只是「所有可能对局」的一个样本。

```
sim-balance      总胜率 ±4.4(500 局) · 单格 ±9.8(100 局)
sim-campaign     每关 ±6(240 局,新默认)
sim-hero-mirror  每个备选 ±2.5(400 局)
```

**两版之间小于半宽的差值不要拿来调参。**

还有两条比半宽更狠的教训,都是这一轮踩出来的:

1. **闸门的判定要配得上它的样本量。** 光印半宽没用 —— 判定本身若是
   「点估计 vs 写死阈值」,它既会被噪声判红,又可能在真出问题时红不了
   (sim-campaign 的落差判定在 60 局下即使真实落差为 0 也只有 z=1.8)。
2. **先验证尺子,再读数。** 对镜类模拟有个免费的自检:
   **两边放同一个东西,结果必须是 50%**。sim-hero-mirror 从来没做过这一步,
   于是带着 26% 的中性点跑了很久,还劝退过两轮设计。
   任何对称的模拟都该先过这一关。

---

## 2. 接下来做什么

标记:🟢 不碰平衡闸门 · 🟡 要跑 sim-balance / sim-campaign · ⏱ 半天以上

### A. 机制(每条单独做,单独过闸门)

> **不要一次做多条。** 已经吃过两次亏:一批改动一起上,矩阵红了查不出是哪条。

#### 🔴 先看这个:**九种条件引擎支持、卡池一张都不用**(2026-08-07 量出来)

下面 1/2/4 三条的共同前置,而且比原来以为的严重得多。
`npm run lint-content` 现在会报(新增 `unused-condition` 规则):

```
ifBoardCount · ifHeroHpBelow · ifHandCount · ifKeywordCount · ifMorale
ifSupply · ifChain · ifField · ifGraveyardCount        ← 各 0 张
ifTurnAtLeast 1 张 · ifEnemyHeroHpBelow 1 张 · ifTroopCount 2 张
```

同时全池有 **145 张卡产屯粮、111 张卡涨士气**。
也就是说这两条资源轴是**只写不读**的:玩家攒了一局的粮,
没有任何一张牌会因此变强。「粮尽惩罚」缺的从来不是惩罚,是**读它的卡**。

这也解释了先手补偿扫描里那件怪事(`supply+2` / `morale+2` 与无补偿逐格相同)——
当时的结论是「预组里没有载体」,现在知道**全池都没有读的一侧**。

为什么以前没人发现:`thin-mechanic` 只数 op,而**条件不是 op**。
新规则的键清单由**编译期断言**守着(漏一个键就编译不过,实测验证过),
所以以后加新条件不会重蹈覆辙。

**✅ 2026-08-07 做完:第二十四卡包「因勢」,九种条件全部有卡在用**(`unused-condition` 9 → 0)。
七张新卡 + 给四张**已有卡**加条件(連環計 / 背水一戰 / 運籌帷幄 / 堅壁清野)——
后者是因为重名闸门当场红了:**那四个名字卡池里全都有,而且概念一模一样**
(連環計 本来就该读「链」)。问题不在命名,在我没先查池子就动手设计;
造第二张同概念的卡正是「两张同效果的卡不该并排站着」那一条。

顺带查实一件影响这条轴设计的事:**粮道每回合末自动 +1**(`reducer` 的回合结算),
而全池 145 张卡产粮、**只有 2 张卡消耗它**。所以 `ifSupply ≥N` 约等于 `ifTurnAtLeast N` ——
第一版写成条件的 足食足兵 实测 **+13.5**,正是因为那根本不是代价。
改成 **軍需 5 的真消耗**(这条轴的第三个出口)再涨到 5 费,才收到 +5.0。

**下一步不是再加条件,是给这两条轴补「代价」那一侧** —— 粮尽惩罚(第 1 条)
现在有了明确的落点:先让粮真的会被花掉,再谈花光了会怎样。

1. 🟢 ~~**粮尽惩罚**~~ —— **2026-08-08 做完(引擎 + 第二十六卡包「糧盡」)**。
   动手前量的三个数决定了做法:**产粮 145 张 · 耗粮 3 张 · 能减别人粮的 0 张**。
   第三个是关键 —— 粮道只涨不跌,所以惩罚写得再好也永远不会触发。
   于是这一条是三件事一起:判据、断粮的出口、耗粮的入口。

   **判据是「边沿」不是「电平」。** 写成「回合开始时粮道为 0 就扣」看着更自然,
   而且大部分用例照样绿 —— 只有开局那条会红:开局粮道**本来就是 0**,
   电平式判据会让先手第一个回合当场挨罚,那跟断粮没有半点关系。
   所以判据是 `before > 0 && after === 0`,放在 `resolve.changeSupply`(粮道的唯一入口)
   而不是各调用点 —— 铁律 7 那三处同步点就是这么裂开的。
   两个方向都跑过变异:判据滑成电平 → 「开局」那条红;删掉提前返回 → 「不重复扣」那条红。
   顺带记一件事:`before > 0` 其实和提前返回**重复**,留着是为了让判据自己写清楚它是边沿,
   别把它当独立防线(注释里写了)。

   **惩罚取士气 −1**,不是掉血:士气全池 111 张写、**只有 2 张读**,和粮道一样是只写不读的轴,
   让粮尽去喂它,一处改动同时给两条轴接上了另一头。士气自己每回合向 0 收敛,
   所以断一次粮是一段时间窗不是永久债;连断两次才过 `MORALE_THRESHOLD`(全场 −1 攻)。

   **卡池先查了一遍,这次查得很值:池子里已经有五张名字就叫「断粮」的卡,一张都没碰粮道** ——
   絕其糧道 `mill 3` · 斷其糧道 弃 1 + 打脸 2 · 焚糧校尉 `mill 2` · 烏巢守將 每回合 `mill 1` ·
   **轉運使**(名字是运粮官)每回合 `mill 2`。「断粮道」在这个卡池里一直是**磨牌的风味词**。
   这不是它们写错了(磨牌那条轴自己是通的),但它是「卡面在说谎」的一个变种:
   **名字**在说谎,而没有任何闸门看得见。这五张这一轮一张都没动。

   第二十六卡包六张:劫糧都尉 +2.5 · 劫糧 +1.5 · 犒軍 +0.5 · 輜重營 +0.0 ·
   因糧於敵 −1.8 · 圍城 −3.0,全部在对照组区间内。回炉三张,其中一条结论要记住:
   **断粮在今天的卡池里几乎不值钱** —— 因糧於敵 第一版纯搬粮实测 −8.3,
   因为粮道每回合自动 +1、而全池只有六张军需卡会因粮不够而卡住。
   所以每张断粮卡都得自带一份不依赖对手构筑的收益。**这个结论会随军需卡变多而过期。**

2. 🟢 ~~**士气崩溃**~~ —— **2026-08-08 做完:溃散**。士气**触底**(= `-MORALE_CAP`)那一方
   **失去守護** —— 阵线散了就没人挡在前面。过线(`MORALE_THRESHOLD`)那一档仍然只是 ±1 攻,
   触底才有第二段;正的那一端**没有**对称物(高涨不是悬崖,崩溃才是)。

   **实现上唯一有意思的地方是它为什么需要一个实例字段。**
   第一版想在 `refreshAuras` 里直接把 `guard` 从 keywords 里摘掉 —— 那是错的:
   `refreshInstance` 每次受伤/治疗/加附魔都会被调,而它**从卡面重新算一遍关键词**。
   在外面摘掉的守護,下一次挨打就长回来了,不报错、不崩,只是那个守護忽然又生效了,
   看起来像随机。所以压制必须和 `shieldUsed` / `stealthBroken` 走同一条路:
   实例上留一个 `routed` 标记(由 refreshAuras 全权管理),由 `refreshInstance` 自己认。
   变异验过:压制搬到 refreshAuras 里去 → 5 条红;只在触底时写、漏掉复位 → 2 条红。

   闸门:`rout.test.ts` 7 条 · sim-balance 逐格无变化 · sim-campaign exit 0。
3. 🟢 ~~**将星陨落**~~ —— **2026-08-08 做完**。传奇武将阵亡时**双方士气各再 -1**。
   新 `GameEvent` `LegendFell`(零状态,只是播报信号);铁律 7 的三处同步都补了 ——
   加进 types.ts 之后 `eventCoverage` **当场红**,那道闸门是有用的。

   **第一版是「失主 -1、斩将方 +1」,它把平衡闸门弄红了。** 那等于把传奇交易的
   士气摆动从 2 格放大到 4 格,而那正好是滚雪球的形状:打得动传奇的一方赚得更多,
   赚了之后身材更好、更打得动传奇。实测 魏武揮鞭 57.8% → **61.8%**,
   还拉出 25% / 74% 两个极化对位。
   现在这一版**双方同吃**,所以「斩掉一个传奇」相对「斩掉一个普通武将」并不多赚 ——
   它不制造优势,只把两支队伍**一起**往溃散那条线上推。A/B 同种子对拍(各 200 局/对):
   有 59.4% vs 无 58.2%,差 1.2pp 落在噪声里。
   副产品是一个真的决策:自己的传奇死了会连带把对面也压一格 —— 苦肉计。

   ⚠️ 顺带记一笔:**魏武揮鞭 本来就贴着 60% 的闸门线**(58.2%),那不是这次改出来的,
   是一条早就存在的欠账(和 坐斷東南 贴 40% 下沿是同一类)。

   另:`dp-15` 在新规则下无解了 —— 那道题的解法要打死自己的孫策(传奇),
   将星陨落把我方士气压到 -2、全场 -1 攻,斩杀线就差那一点。
   按 `lp-beishui` 的先例**修的是题不是卡**:`SEEDS=800 KEEP=60 npm run mine-puzzles` 重挖。
4. 🟢 ~~**同袍上桌**~~ —— **2026-08-08 做完(第二十八卡包「同袍」)**。
   一条此前没人写下来的实现约束:`friendlyBattle` 数的是「与**来源卡**同赴过一场仗的友军」,
   所以**来源卡自己得带 `battles`**,否则恒为 0。于是三张全是以那一场仗为名的锦囊,
   `battles` 写它自己那一场 —— 零引擎改动,卡面读起来也自洽。
   威震逍遙津 +2.5 · 官渡相持 +0.2 · 同舟 −4.2。

   **这一包的做法值得照抄:先证明它在数,再看它多强。** 动手调数值之前先写了
   `engine/battleCount.test.ts` —— 「来源卡自己没名单」那种卡不报错不崩,
   只是效果永远停在 0 层,而模拟只会告诉你「它偏弱」,**不会告诉你它根本没在工作**
   (壁中書 第一版就是这么白调了三轮)。

   ⚠️ 另记一次**我在追噪声**的教训:中途连做三次改动,Δ 分别动了 0.7 / 4.0 / 0.5,
   而 400 局下两次测量之差的标准误就是 ±3.5 —— 那三次「变化」一次都不算数。
   改用 600 局、一次只动一张卡之后数字才开始说话。
5. 🟢 ~~**家族「夷三族」**~~ —— **2026-08-08 做完(第三十卡包)**。家族这条轴上线以来
   **只有正收益**:凑族人是纯赚的,对手没有任何理由让你别凑。现在它有了对手的那一半。
   引擎侧只加了一个目标 `clanOfChosenEnemy` —— 卡池里第一个**由「选中的那一个」
   派生出「一组」**的目标(此前 chosen\* 一律一对一、all\* 一律不给选)。
   夷三族 +4.7 · 連坐吏 +3.7。

   **两张都没有因为「上限很吓人」而被削**,理由值得记:曹氏 27 人是**卡池里**的
   族人数,不是**场上**的。真正打中三个以上要对手主动把三个同族摆在一起 ——
   所以它们的强度是**对手构筑的函数**:打普通牌组是单体解场,打家族流才是族灭。

   ⚠️ 最容易漏的一处已经钉了测试:这条目标必须进 `CHOSEN_TARGETS`。
   漏在外面**不报错** —— 那张牌会被判成「不需要目标」,于是 chosen 恒为 undefined、
   目标解析返回空,**这张牌恒定什么都不做**,而卡面照常显示。
6. 🟢 ~~**降将机制**~~ —— **2026-08-08 做完(第二十七卡包「歸義」)**。
   65 个人的名单在 `overrides/defectors.ts`(committed 常量 —— 那份史料是懒加载的,
   让 `cards.ts` import 它会把整份 lore 拖进首屏)。引擎侧只加了
   `CountSource.friendlyDefector`(**含自己**,和 friendlyDynasty 相反)。
   五湖四海 +2.5 · 棄暗投明 +1.5 · 不念舊惡 −2.0。

   **部族 payoff 的定价通则(三张全部回炉才收敛出来的):
   印在卡面上的身材要按同费白板给足,部族那一层是纯粹的上不封顶。**
   第一版三张都把部族收益当成身材的一部分预扣掉了,于是在 0 层的预组里全塌成死牌
   (−13.8 / −7.8 / −3.0)—— 为一个大多数牌组里不存在的收益提前付了钱。

   ⚠️ **这条轴的 Δ 结构性偏低**:六套预组里的降将数是 0/1/0/0/3/0,
   所以 payoff 在 sim-cards 上几乎永远只吃到 0~1 层,量到的是**地板**不是强度。
   上限只能靠形状约束(避开「全体 × 每有一名」那种二次方形状)。

   顺带补上一个洞:**卡池快照此前看不见部族标签**。贴完 65 张 defector,
   快照 diff 只有 3 行 —— 65 张卡的战力被改了而闸门一声不吭。
   `digest` 现在带一段可读的 `{troop,defector,clan,battles}`。
7. 🟢 ~~**师承机制化**~~ —— **2026-08-08 做完,而且它和第 23 条是同一件事**。
   师承的形状**就是**羁绊的形状(两个人同时在场、彼此变强),再造一套平行子系统
   正是这个仓库反复吃亏的那件事。所以只加条目、**零引擎改动**:
   38 条 `mentor` 边里两边都在池且都没挂羁绊的有 37 条,取 21 条,羁绊 31 → **52**。
   两条自我约束写在 `overrides/bonds.ts` 的那一段头注里(孔门只取八个;声明在弟子身上)。
8. 🟢 ~~**阵形补四种**~~ —— **2026-08-08 做完(第二十九卡包「八陣」)**,
   而**真正该修的是门槛,不是数量**。原有四种里 长蛇要满员、鱼鳞要三个同兵种,
   门槛高到实战里基本见不到,所以真正在跑的只有锋矢与鹤翼。
   补的四种(偃月/方圓/雁行/衡軛)全部只看战线人数与位置,门槛**统一是 3**。

   **这一包真正的结论:阵形的强度几乎全写在门槛上,不在增益数值上。**
   第一版门槛写 4,三张全在带外(−7.3 / −3.0 / −10.5);一个字没改增益、
   只把门槛降到 3,就变成 −1.2 / −1.2 / −6.8。
   最终 雁行 +6.5 · 衡軛 +5.3 · 偃月 −1.2 · 方圓 −1.2。

   两条设计上的用意:**摆位第一次真的有讲究**(雁行摆在最右一个人都吃不到);
   **衡軛 是 鶴翼 的补集**,两张同场正好覆盖全场 —— 这条轴上第一个真正的组合。
9. 🟢 ~~**高费段扩充**~~ —— **2026-08-08 做完(第三十一卡包「定鼎」)**,
   而真正的缺口比这条写的更具体:**那 70 张全是武将,锦囊一张都没有**。
   顶费段只有大身材、没有大场面,而 CCG 里最被记住的往往是后者。
   三张走三个方向:焚舟(掀桌)· 登壇拜將(铺场)· 傳國玉璽(把已在场的抬起来)。
   最终 焚舟 +1.3 · 登壇拜將 −2.7 · 傳國玉璽 −3.3。

   ⚠️ **一条被实测推翻的预判,留着当反例。** 我在这一包的头注里写过:
   「十费的牌要能打出来对局得先撑到第十回合,而这把尺子的对局中位长度远短于此,
   所以它量到的是『大多数局里它是一张卡在手里的废牌』。」听起来很有道理 ——
   而第一轮实测是 **焚舟 +27.3 · 登壇拜將 +10.8**。九费牌根本不是废牌。
   **「这一档天生难看」这种话必须先量再说**,否则它会变成一张免死金牌,
   让整个高费段悄悄超模。

   焚舟 的三版轨迹本身是一条定价笔记:
   `消滅所有敵方武將 +27.3 → 單側全場 5 點 +18.7 → 雙方全場 5 點 +1.3`。
   前两版都是单侧,而在九费上单侧清场**不是代价问题,是形状问题** ——
   一张无条件把对面桌子掀掉的牌,贵到几费都还是无条件的。
10. ⛔ **锦囊播种** —— **这台机器上做不了,不是不想做**:它要改
    `scripts/seed-mechanics.ts`,而那个脚本和 `scripts/import-content.ts` 一样,
    **没有姊妹仓库 `../ThreeKingdomMastersIOS` 就不能动**
    (见第 0 节「唯一的例外:素材源头」)。这台机器上没有那份素材。
    在有素材的机器上做,或者继续走手写卡包这条路 —— 第二十四到二十九包
    一共二十七张手写锦囊,已经比播种能产出的更贴题。
11. 🟢 ~~**四条冷门 op 补卡**~~ —— **2026-08-07 做完(第二十五卡包「授受」)**。
    addToHand / banish / shuffleIntoDeck / transform 原本各只有 1–2 张,现在各 3 张以上,
    `lint-content` 的 thin-mechanic **整条消失**。七张新卡全部落在对照组区间(−4.8 ~ +6.7)。
    动手前先把四条 op 的现有用例列出来照着**避开**(上一包的教训):
    banish 走不指定目标、shuffleIntoDeck 走 `side: 'friendly'`(那一支此前零张)、
    transform 走友方升级 —— 现有两张都是变羊,不再造第三张。

### B. 模式与系统

12. 🟢 ~~**难度选项**(简单 / 标准 / 史实)~~ —— **2026-08-08 做完**。
    簡易 = 关底血量 ×0.75 · 標準 = **恒等式** · 史實 = ×1.3。

    **只有一个旋钮,而且必须是血量** —— 手上看起来有三个,后两个都不能用:

    - **`deckTier` 对强度是非单调的**,而且是量出来的:于謙 那条曲线是
      `0:20% · 0.15:69% · 0.3:35% · 0.45:25% · 0.6:69% · 0.75:51% · 0.9:39%`。
      拿它当难度旋钮,「调简单一点」有一半概率是调难了,**而且每一关拨的方向还不一样**。
      它是网格搜出来的定值,不是刻度。
    - **`RunModifiers` 有优先级冲突**:试炼的修正必须原样保留(它摆的是胜负目标那个单位),
      傳承的修正在试炼时本来就要让位;再插一层难度修正,三者的优先级会变成一张没人说得清的表。

    **标准档是恒等式不是「默认值」,是契约** —— `sim-campaign` 那条曲线是几十轮网格搜出来的,
    标准档只要不是恒等,那条曲线连同 ROADMAP 里所有关于它的结论当场作废,
    而且**不会有任何东西报警**(闸门测的仍然叫「标准档」)。有测试逐关钉着,
    落地后重跑 sim-campaign 也确认逐格无变化。

    ⚠️ 这一条补 bug 补出一条 e2e:第一版只把难度接进了**开打那一行**,
    列表与简报仍然显示卡面基础血 —— 选了「史實」屏幕上还写着 30 HP,而真打是 39。
    那不是显示问题,是**卡面在说谎**。`e2e/campaign-mode.spec.ts` 现在盯着这件事。
13. 🟢 **战役模式** —— **2026-08-08:这条的前提是错的,做法改了,并落了四场**。

    原文说「双方阵容从索引取是现成的」。查下来两条都不成立:

    1. **索引里没有阵营信息。** `battles` 是一张扁平名单,没有「谁在哪一边」。
       按朝代分也不行 —— 安史之亂 十九个人全是 `tang`(郭子儀 和 安祿山 同标签),
       長平 六个人全是 `warring-states`(白起 和 趙括 同标签)。
    2. **更要命:索引是单边的。** 官渡 十一个袁绍的人对**两个**曹操的人 ——
       因为「生平里提到这一仗」的多半是那一边的人。
       照它取双方阵容,一边会没有人。

    所以「另起一个模式」是错的方向。**真正的缺口是覆盖率**:
    索引里 24 场,而「名局重现」只有 14 场,两者**只重叠 7 场** ——
    也就是说十七场著名战役在时代长卷上看得见、却打不了。
    名局重现已经写好、有测试、有 `sim-history`,该做的是**把它补齐**,
    而不是在一份支撑不住的数据上再建一个模式。

    这一轮补了四场:**馬陵 53% · 襄樊(水淹七軍)60% · 夷陵 43% · 淝水 48%**,
    全部落在 35–68% 内。(报告里那六场「太送」是动手之前就在的,不是这一轮的。)

    ⚠️ **量出来一条以后写斩将局能直接用的:主帅血量在斩将局里是错的旋钮。**
    襄樊 血量 42 → 54,胜率 **78% → 78%**,一动不动 —— 赢法是斩下目标,不是打空主帅。
    在目标旁边加两道守护墙也照样 78%。真正的旋钮是**目标自己有多硬**:
    把目标从 2/2 的禁軍换成 5/10 守護 的敵軍主將,一步 **78% → 3%**。
    (2/2 的斩将目标会被任何一发群伤顺手带走,于是这局比普通局还容易。)

    ⚠️ 中途还栽了一次「一次动两个旋钮」:撤掉陪衬的禁軍**同时**给玩家加手牌与护甲,
    3% → 83%,分不出是哪一个。分开量才知道两个都很沉(主將单独 + 玩家原配 = 60%)。
14. 🟢 ~~**冒险第四章**~~ —— **2026-08-08 做完:第四章 · 立法者(24 → 32 关)**。

    前三章打的都是提着刀的人;这一章打的是**改规矩的人**:
    管仲 · 商鞅 · 李斯 · 桑弘羊 · 王猛 · 王安石 · 張居正 · 雍正 ——
    没有一个是靠武功留在史书上的,而他们改动的东西比任何一场仗都活得久。
    主公技刻意全部走资源与规则,不走打脸。四句台词全取原话。

    最终曲线 **63 · 49 · 48 · 62 · 65 · 44 · 38 · 15**,`sim-campaign` 四章全绿。

    ⚠️ **量出来三件事,一件比一件值钱:**

    1. **我自己造的一个 bug:三关的主公技是空转的。** 管仲/桑弘羊/雍正 原本给的是
       「糧道 +N」,而**同一天我刚把军需卡整个排除出 Boss 卡组**(见坑 12)——
       那些粮永远花不掉。桑弘羊 因此 95%。换成护甲/加血后立刻 95% → 87%。
       **给 AI 的东西要先问「它用得上吗」。**
    2. **血量在这一章几乎是平的旋钮。** 雍正 66 → 82 → 104 血,只换来 65% → 57% → 54%;
       桑弘羊 +16 血只动 2 个点。玩家场面已经压倒时,加血只是把局拖长。
       (`tune-campaign` 的文件头早就写过这条,我又独立撞了一次。)
    3. **真正的旋钮是 `deckTier`,而它非单调 —— 只能网格搜。**
       王安石 的曲线是 `0:50% · 0.15:90% · 0.30:77% · 0.45:63% · 0.60:94% · 0.75:85% · 0.90:90%`。
       `ONLY=… npm run tune-campaign` 一轮搜完八关,照抄回去,雍正 54% → 15%。

    另:两道闸门当场抓到我漏的 —— **章内血量必须递增**(桑弘羊 68 之后那三关还停在 54/56/60)、
    以及 `economy.test` 的「试炼与连斩的顶格 = 关卡总数」(24 → 32)。
    后者的注释里写着上一次扩章也是被它抓的 —— 这次是第三次。
15. 🟢 ~~**每日挑战多样化**~~ —— **2026-08-08 做完:每日名局**。

    每日那一屏此前只有斩杀谜题:三道残局,做法一样、思路一样 ——「这一手怎么打死他」。
    它是好题,但连着做三十天就只剩计算。名局问的是**完全不同的问题**
    (带自己的牌去接一个历史处境),而它**本来就在库里**(24 场),
    只是藏在另一个入口后面,大多数人一个月也点不进去一次。

    两条刻意的克制:
    - **不发额外奖励。** 名局自己有首通奖励;再叠一层「每日名局奖励」就是在动经济曲线,
      而这一条要解决的是**可见性**,不是产出 —— 加个入口,不加个货币。
    - **只是入口,不就地开打。** 名局要带你自己的卡组,而斩杀那一屏一张牌都不用选;
      把选卡组那一步搬过来就等于把名局屏抄一遍。

    `dayHash` 提到了 `dayKey.ts`:谁需要「按日期确定性地挑一个」都该用同一个哈希。
    (`dailyPuzzle` 里那份先写的留着不动 —— 它已经决定了历史上每一天的题面,
    改哈希等于把所有人的「连续 N 天」记录重排一遍。)

    测试的判据是**确定性 + 会轮换**:只钉确定性会漏掉一种烂法 ——
    哈希写死成常数,每天都是同一场,而它照样是「确定的」。
16. 🟢 ~~**牌组模板库**~~ —— **2026-08-08 补完最后一轴**,但做法和这条写的不一样,
    值得记:**羁绊 / 家族两轴早就做完了**,而且不是「二十套静态模板」,
    是 `deckSuggest` 的**按种子现场生成**(只用你拥有的卡,建议出来立刻能存能打)。
    静态模板做不到那件事 —— 给一副含未拥有卡的「理想卡组」,点保存就被校验挡回来。

    这一轮补的是**兵种**那一轴,外加**降将**(这一轮新做的部族)。

    ⚠️ 它们和前两轴有一处结构性差别:**种子不是名单,是判据**。
    羁绊/家族先有一份 id 名单,兵种/降将符合的人有一两百个。
    于是多一个前两轴没有的失败模式:**种子铺满整副牌**,曲线那一步等于没跑,
    产出「一堆同类卡」而不是「一副能打的牌」。`TRIBE_SEED_CAP = 18` 挡的就是这个,
    测试里有一条专门钉它(去掉上限,`advisor` 立刻塞满 30/30)。

    降将单独摆一条:它是唯一一条**跨全部六个主义**的部族,任何主公都组得起来。
17. 🟢 ~~**成就隐藏档**~~ —— **2026-08-08 做完(三条)**,但**没有**按原来设想的
    「用关羽单挑击败颜良」那种指名道姓的条件做,理由值得写下来:

    隐藏档的作用和普通成就是**相反**的。普通成就是**指路**(功名簿里摆着
    「伏兵触发 25 次」,至少他知道有这么个东西);隐藏档是**惊喜** ——
    你在做自己想做的事,游戏忽然告诉你「这件事有名字」。
    所以隐藏的必须是**你不冲着它也会做到**的事。
    「用关羽单挑击败颜良」不是 —— 那要同时抽到两张特定卡、还得走单挑,
    不查攻略永远碰不上。那不是隐藏成就,是**没写说明的任务**。

    三条挂在这一轮新做的机制上(粮尽 / 溃散 / 斩传奇),都是对局里自然会发生的事。
    实现上**零新事件**:三条全从既有事件流推(`SupplyChanged` 掉到 0、
    `MoraleChanged` 触底、`LegendFell`),口径一律只数**敌方身上**发生的那一次。

    ⚠️ 顺带一提,`achievement.test.ts` 那道「加了成就却忘了埋点」的闸门这次
    **抓到了一个我自己造的 bug**:改 `tallyStats` 时手一滑把 `CardDiscarded`
    整条删掉了,它当场点名 `ach-mill 依赖的 cardsMilled 没有任何地方在埋点`。
    那类 bug 的表现是「永远 0/N」,没有闸门的话没人会发现。
18. 🟢 ~~**新手前三十分钟串线**~~ —— **2026-08-08 做完:新兵之路(四步)**。

    **缺的从来不是内容,是顺序。** 教学对局、兵法讲堂、讲堂实练、六套预组、构筑器
    全都做好了,而且各自都不差 —— 问题是它们**互相不知道对方存在**:
    教学打完之后没有任何东西说下一步该干嘛,于是新玩家回到标题页,
    面对三十多个入口自己猜。猜错的那些人不会有第二天。

    四步:`走一遍教学 → 用预组打一局 → 讲堂实练做一道 → 组一副自己的牌`。
    最后一步是分界线 —— 组过一次牌的人就有了自己的东西,之后往哪走该由他自己定。

    三条设计上的自我约束:
    - **判据是状态,不是顺序。** 写成「上一步做完才算下一步」很自然,而且大部分
      用例照样绿 —— 只有「先自己组了牌、还没打过一局」那种人会撞上:他明明组过了,
      清单却说他没有。这是**指路**不是**关卡**。
    - **不新增任何存档字段。** 四步的判据全是别处已经在持久化的状态,
      所以老玩家一打开就是「四步全绿、整块不显示」,不会凭空冒出一张任务清单。
    - **教程邀请还挂着时这条路不显示** —— 它俩的第一句话是同一句。
      邀请是「第一件事」的强提示,这条路是「然后呢」的答案,时序上本来就该错开。

### C. 内容(手写,零平衡风险)

18.5. 🟢 ~~补 25 处「卡面在说谎」的文案~~ —— **2026-08-06 全部补完**。
    `CardFace` 渲染的**只有 `def.text`**(这个仓库没有「从脚本生成描述」那一层),
    所以卡面上没写的机制玩家就是不知道 —— 太史慈的战吼是「对随机一名敌将造成
    3 点伤害」,而卡面只写着「碾壓。神射之勇,一往無前。」。
    **三分之二的缺口根本不用写文案**,值得记住这三种补法:

    | 补法 | 张数 | |
    |---|---|---|
    | **改合并方式** | 4 | `FLAVOR_OVERRIDES` 只有 `text` 一个字段,走普通 spread 就等于「补一句风味 = 删掉规则说明」。改成追加(`withFlavorText`),四张一起好 |
    | **删掉多余的 text** | 2 | 正确的文案一直在 `signature-skills` 里,pack12 想加个「碾壓」却整段重写、又合并得更晚 |
    | **真的补写** | 13 | 照卡池里同机制卡的既定句式 |

    ⚠️ **先量再改**:那六张里有四张带着「看起来像残留」的机制
    (杜預 甚至藏着一个全场 +0/+1 并获得守护的光环),600 局实测却是
    杜預 +2.0 · 李廣 +0.0 · 羊祜 +3.2 · 勾踐 +3.5 —— **全都平衡**。
    照「像残留就清掉」的判断动手,会一口气削弱四张好卡。**问题在文案,不在数值。**
    闸门(`overrideConflict.test.ts`)的名单已清空,新写的卡再犯当场红。

19. 🟡 **名言** 253 → **267**(2026-08-08 补 14 条)—— **没到 300,而且这条目标本身要改**。

    签名卡里缺名言的有 140 张。第一版草了 22 条,**自查之后只留下 12**,
    补齐格式后落库 14 条。砍掉的那 10 条是最容易混进来的几类,值得逐类记下:

    | 类型 | 例 |
    |---|---|
    | **不是他说的,是别人说他的** | 郭子儀「國家再造,卿力也」是代宗**评价他**;徐達那条是《明史》**史臣评语** |
    | **张冠李戴** | 「少松江鱸魚」出自《后汉书》,是**曹操**说的,左慈只答了「此可得也」 |
    | **是叙述不是台词** | 吳廣 那条「失期,法皆斬」是《陈涉世家》的**旁白** |
    | **伪托** | 張獻忠「七杀碑」来源存疑,和《入川題壁》同一类 |
    | **记得有这回事,复述不出原文** | 虞允文、王世充、于吉 —— 三条都属于这一档 |

    ⚠️ **所以「253 → 300+」这个目标定错了。** 剩下的 128 位大多是割据军阀、
    五代节度使、唐末流寇,汉文史料里**只有事迹没有原话**。
    再补几十条只有两条路:降低出处标准,或者编。两条都比「停在 267」糟得多。
    这条待办该改成**「把有原话的都补完」**,而不是一个数字 ——
    数字目标会逼着人去凑,而这个游戏的全部可信度就压在这一条上。

    另:`loreQuotes.test` 那道「每条名言都得标出处」的闸门这次当场抓了我两次 ——
    一次是注释写在条目**外面**(它只看条目内头四行),一次是「韩愈《师说》」
    只有六个字、短于它要求的八字下限。判据看起来很死板,而它拦住的正是
    「随手写一句『出自某书』就贴上来」。
20. 🟢 **出战台词** 307 → 400+(源头 `officerLines` 只有 taunt/ult 两类各 147 人,已挖尽,只能手写)。
21. 🟢 **兵器坐骑** 19 → 60+(**源头生平里具名兵器一条都没有**,实测过,只能手写)。
22. 🟢 ~~**宿敌** 41 → 60~~ —— **2026-08-08 做完**。素材是关系网里现成的 `foe` 边,
    每条注释与 `RIVAL_LORE` 里那句都是生平原文。sim-balance 200 局/对逐格无变化。

    筛法里有一步**只能人来判**:关系网里有几条 `foe` 其实是**同僚**
    (高覽 / 張郃 是一起降曹的,那条边来自郭图传里那句谗言),照单全收会写出
    历史上说不通的宿敌。自动抽出来的关系**不等于**能直接用的关系。

    另外 `rivals.test.ts` 当场抓到一件事:每条宿敌都必须有 `RIVAL_LORE`。
    十九条只写了机制没写史料,测试逐条点名报了出来 —— 这道闸门守的正是
    「宿敌是唯一一条会让玩家去查史料的机制」那个立项理由。
23. 🟢 ~~**羁绊** 31 → 50~~ —— **2026-08-08 做完(31 → 52),和第 7 条一起**。
    素材是关系网里现成的师承边,每条都带生平原文。预组里一个弟子都没有
    (只有孔子本人),sim-balance 200 局/对逐格无变化。
24. 🟢 ~~**卡面风味句**~~ —— **2026-08-08 做完:97 张,全池空白卡面归零**。

    先量再写:全池 2,451 张可收集卡里,卡面**完全空白**的只有 97 张,
    而且**全部是纯白板**(无关键词、无战吼、无光环)。带机制的卡至少有一行规则说明,
    白板卡什么都没有 —— 玩家看到的就是一个名字加两个数字。
    这个游戏卖的是「这些人是真的」,而这 97 张恰恰是最不像真人的那一批。

    写作标准同 `lore-quotes`:每一句都从那个人的生平里取,**不编心理活动**。
    引号里的话(「受將軍任,在此禦寇,何敢委去」「苟利國家生死以」「春花秋月何時了」)
    都是原话;拿不准的写他做过的那件事,不写他「如何想」。

    ⚠️ **和 `FLAVOR_OVERRIDES` 分开放**:那份是人工挑的十几条顶级传奇,
    这份是**穷举**出来的一整批(判据是机械的:`text` 为空)。混在一起,
    下次再想问「还有多少张没有文案」就得先把它们分开。两份走**同一条追加路径**
    (`withFlavorText`)—— 走普通 spread 的话「补一句风味」等于「删掉规则说明」。

    ⚠️ **闸门的判据是从卡池那边数,不是从表那边数**:表里写满一百条而合并层没接上,
    那张表照样是死的。`vanillaFlavor.test.ts` 断言的是「全池没有一张卡面是空白的」
    以及「表里每一条都真的出现在了卡面上」。

    另注:`npm run audit-en` 的卡面文案覆盖率从 `2354/2354` 变成 `2451/2451` ——
    分母涨了 97,因为那 97 张此前根本不在「有文案」的统计里。
    首屏余量 26.4 → **19.4 KB**(这一批约 7KB),仍在预算内,但这条轴的余量不多了。
25. 🟢 ~~**冒险关卡过场文**~~ —— **2026-08-08 做完(24 关各一段结局)**。

    **intro 是「他现在是谁」,outro 是「他后来怎么了」** —— 而后者的力量恰恰来自
    **你刚刚打赢了他**:白起在牌桌上是最狠的一关,而他的结局是杜郵那一柄劍。
    所以它只在通关之后显示,放进 intro 就成了剧透(两个方向都有 e2e 钉着)。
    这个游戏的素材本来就带着这层反差,缺的只是一个把它交出来的时刻。

    写作标准同 `lore-quotes`:**只写史书里查得到的结局**,不替他们编心理活动。
    引号里的话(「莫須有」「天亡我,非戰之罪」「我固當死」)都是有出处的原话。

    ⚠️ `outro` 写成**必填字段**,于是 tsc 替我数那二十四段 —— 漏一段不会报错、
    只会有一关静悄悄什么都不给。测试再补 tsc 看不见的那一半:
    不许和 intro 是同一句、二十四段不许重复(复制粘贴不会报错,只会让那关白写)。

**手写史料的两条标准**(见 `src/content/overrides/lore-quotes.ts` 文件头):
- `quote` 名言 —— **必须有确切出处**,注释里标明。拿不准宁可空着。
  伪托同样不算(石達開《入川題壁》就是因此没写)。
- `line` 台词 / `arms` 兵器 —— 是游戏风味或演义,可以写,但注释里要标明出自演义还是正史。

### D. 界面

26. 🟢 ~~关系图谱按 kind 筛选~~ —— **2026-08-08 做完**。同一天把羁绊 31 → 52、
    宿敌 41 → 60,边数几乎翻倍,而弦图的好处(「谁的线多一眼可见」)是**第一个**
    被边数吃掉的 —— 尤其是同一对人既有羁绊又是宿敌的那几对(孙膑/庞涓),
    两条弦叠在一起根本分不出来。
    **筛的是边不是点**:被筛掉那一侧的人仍然画在圆周上(位置由全集算,不随筛选变)。
    布局一变,这张图就没法用来对比两种关系的疏密了,而那是它唯一比列表强的地方。
27. 🟢 ~~家族树可视化~~ —— **2026-08-08 做完(`ClanTree`)**,但**不是族谱树**,理由要记住:

    `kin` 边**没有方向** —— 关系网只记「这两个人是亲属」和那句原文,不记谁是父谁是子。
    硬要分上下层就得靠猜(生卒年?名字?),而**猜错的族谱比没有族谱更糟**:
    它看起来像是查过的。所以画成无向的**自我中心图**:本人居中,有明文亲缘的绕环。

    **它比名单更诚实,而不只是更好看。** 名单把同族的人并排摆着,读起来像
    「这些人彼此都有关系」;而史料只记明了一部分。图上画的是有明文的那些连线,
    剩下的人仍然列出来,措辞是「同族,史料未記明與**此人**的親緣」。
    (曹操 二十条,曹丕/曹昂/曹叡 等六人反而没有直接明文 —— 那是数据的实情。)

    两处返工都值得记:
    - **第一版用 `relationsNow()`,画出来每个族都只有一个人。** 那个缓存要
      `loadLore()` 之后才有内容,而**列传屏从来不调它**(它是静态 import `lore.gen` 的)。
      于是恒返回空数组,组件一声不吭地把 **274 条族内亲缘全说成了 0**。
      不崩、不红 —— 这个仓库最贵的那类 bug。e2e 现在断言**连线数 > 5**,不只是「图在」。
    - **第一版布局照抄了关系图谱那张弦图**(所有人平摊在圆周上),画出来是**一把扇子**:
      二十五条线从圆周上的一个点扇出去,名字全叠。弦图适合「整张网络谁是中心」,
      而这里的中心是**已知的**。中心已知的时候,自我中心图才是对的形状。
28. 🟢 ~~时代长卷标出战役~~ —— **2026-08-08 做完,而且它顺带查出了一个真 bug**。

    素材是卡面上现成的 `battles`(142 张),**不用碰那份 144KB 的懒加载列传**。

    #### 🔴 顺带查出:战役名单被**假匹配**污染了,而且它影响的不只是长卷

    做归组时发现**明清那一块一场仗都没有**,而明清有七张卡带着 `battles` ——
    逐条一看,那七条全是名字撞车:**鄭和 在昆陽之戰、朱棣 在漠北之戰、
    李鴻章 在合肥·逍遙津**。

    根因:生成层是按**字面**从生平原文里认战役的,而战役名多半是**地名**。
    二十四场里十场跨时代,查完是三类:

    | 类型 | 例子 |
    |---|---|
    | **籍贯** | 楊行密/包拯/李鴻章/聶士成 都是**合肥人**;田豐/張角 是**鉅鹿人**;鄭和 是**昆陽**人;李白 卒于**采石**矶 |
    | **同名不同代** | 哥舒翰、封常清 打的是 **756 年**那场潼關;孫傳庭 是 **1643 年**那场;竇建德 是 **621 年**的虎牢 |
    | **常用词** | 曹叡 与 石虎 进「土木堡之變」,因为两人都以**大興土木**闻名 |

    ⚠️ **必须手查,不能按「取众数」自动清洗** —— 十条里有三条是对的,
    而且恰恰是众数会删错的那三条:慕容垂 真的在淝水、**于謙 是土木堡唯一对的那个**、
    朱棣/王保保 那份名单说的本来就是明初北征。
    「自动抽出来的关系不等于能直接用的关系」—— 和宿敌那一批同一条教训。

    修表在 `overrides/battle-fixes.ts`(手查过的排除表,形状同 `defectors.ts`),
    走合并层的一趟。生成层禁止手改,而认战役的逻辑在姊妹仓库的导入脚本里,
    这台机器上动不了。

    **它影响的不只是长卷**:`CountSource.friendlyBattle`(第二十八卡包)数的就是这份名单 ——
    修之前「合肥舊人」里站着李鴻章和聶士成。修完复测 威震逍遙津 仍是 **+2.5**(逐位相同,
    被删掉的四个人一个都不在预组里),所以那一包的定价不受影响。

    另注:**卡池快照这次立了功** —— 因为 2026-08-08 早些时候刚把 `battles` 加进
    digest 的可读标签段,这十六张的改动在 diff 里一目了然。加之前它是看不见的。
29. 🟢 ~~收藏进度按新档案字段~~ —— **2026-08-08 做完**。图鉴的「收藏度」里多一段
    「檔案」:名言 / 出戰台詞 / 兵器坐騎 / 絕命詩 各自的收集度。

    它和上面那条时代进度问的**不是同一个问题**:时代进度对每张卡是等价的
    (有名言、有绝命诗的卡和纯白板一样重),而这个游戏最不可替代的东西恰恰是那些字段。
    一句话:**时代进度奖励开包量,档案进度奖励你收到了谁。**

    ⚠️ 判定层 `loreProgress` 是**纯函数、数据由调用方喂进来**,这是故意的:
    `lore.gen` 是 144KB 懒加载的,而 `collectionGoals` 被收藏屏与讲堂屏**静态** import ——
    自己去 import 它等于把那 144KB 拖回首屏(`perf-budget` 会当场红)。

    顺带:这四条进度条的分母就是 C19–C21 的现状(名言 253 · 台词 307 ·
    兵器 19 · 绝命诗 79),等于给那三条待办装了个看得见的进度表。
30. 🟡⏱ **暗色 / 亮色主题** —— **2026-08-08 量过了,先装了尺子,主题本身没做**。
    `npm run theme-audit`,判定层 `scripts/themeAudit.ts` 带 13 条测试。

    调色板确实是集中的(`index.css` 里 15 个颜色变量),照理说加一组
    `:root[data-theme='light']` 覆盖就完事。而一量:**56 个 CSS 模块里有 1,889 处
    写死的颜色,一个模块都没漏掉**。只翻那 15 个变量,得到的是「深色底的字压在
    浅色底上」—— 一个字都读不清,而且不报任何错。
    **所以这一条不是「还没排上」,是「直接做会做出一个坏的亮色模式」。**

    尺子把 1,889 分成两档,这一步很重要:

    | | 数 | |
    |---|---|---|
    | **挡路** | **561** | 不透明的 `color` / `background` 字面量 —— 直接决定字读不读得出来 |
    | 不挡路 | 1,359 | 阴影、描边、半透明叠加 —— 在深浅两种底上都成立,**不用还** |

    已经不欠债的文件 7 / 57。欠得最多的:`TitleScreen 54 · MatchScreen 44 ·
    GeneralToken 41 · HeroPlate 36 · CardFace 34`。

    **第一笔已经还了(2026-08-08):561 → 507。** 还的是「值和调色板逐位相同」
    的那 54 处(散在 30 份文件里,同一个 `#d4a84a` 写在 19 个地方)——
    这部分**可证零视觉变化**:每个变量都只在 `:root` 定义过一次、没有任何窄作用域
    覆盖,所以 `var(--gold-mid)` 处处解析成 `#d4a84a`(有 e2e 读 computed style 钉着)。

    ⚠️ 而这一笔也量出了剩下那 507 处的性质:**它们不是机械替换能解决的**。
    559 处里只有 58 处在调色板里现成,其余五百多是自定色(`#c8ae6e`、`#33240c`、
    `#e6c878` 这类)——「它在亮色下该变成什么」是**设计判断**,一处一处地想。
    所以后面这段是设计工作量,不是重构工作量,估工时别按 507 乘一个小数字。

    **怎么还**:把那 561 处逐个换成 `var(--…)`。这是**零视觉变化**的重构
    (深色值不变),所以可以一份一份来、每份截图对拍;数字降到 0 之后,
    亮色主题才真的只差一组变量覆盖。这也是 uiKit 那一轮的做法(先装闸门再动按钮)。
31. 🟢 ~~色盲模式~~ —— **早就做完了,这条清单过期了**(2026-08-08 核对)。
    `settingsStore.colorBlind` → `main.tsx` 写 `document.documentElement.dataset.colorblind`
    → `CardFace` / `GeneralToken` 的 `:root[data-colorblind='true']` 规则接管:
    稀有度玉印补形状、场上单位的「可攻击 / 可被指定」两圈光补形状。
    主义符号(`DOCTRINE_GLYPH`)是**一直开着**的,不受这个开关管。
32. 🟢 ~~字号档位~~ —— **早就做完了,这条清单过期了**(2026-08-08 核对)。
    做的是 `settingsStore.uiScale`(界面缩放滑块),不是纯字号 ——
    理由见 `main.tsx` 那段:全站字号几乎都写成 `clamp(px, vh, px)`,
    单改根字号对它们**一点作用都没有**。
33. 🟢 ~~横屏平板布局~~ —— **2026-08-08 量过了,而结论和这条的措辞不一样**。

    **平板横屏(1024×768)本来就是好的** —— 逐段量过:两排令牌完整落在战场里,
    双方主帅、手牌、结束回合都在位。这一屏几乎全用 `clamp` / `vh` 排版,
    宽高一变它自己会跟着走。所以「横屏平板布局」这条待办的前提是错的:
    要改的不是**平板**,是**矮**。

    真正会坏的是矮视口(手机横屏 ≈390px 高),而坏的方式很具体:
    **绝对定位的浮层按百分比落位,而百分比在矮屏上会正好落在战线上。**

    ⚠️ 回合横幅就是这么错过**两次**的:`38%` 压着敌方那一排,
    上一版改成 `48%` —— 而 48% × 390 = 187px,我方那一排正好在 171–221。
    **碰撞只是从对面挪到了自己身上**,而这一边更要命:你看不见的是你正要操作的单位。
    两排之间只有一条约 30px 的分界带,所以必须**同时**挪到带中央并且瘦下来 ——
    只做其中一件都还压着人(两个方向都跑过变异:只挪不瘦压住**两排**)。

    `e2e/landscape.spec.ts` 现在断言的是**横幅与任何令牌之间留得出 ≥3px 余量**,
    不是「横幅在」—— 出错的那两版横幅都在。
    (2026-08-09 收尾:落点已改成按战场几何算,五个视口档位守着 —— 见第 48 条。
    只验「不相交」也不够:844x390 曾以 4.4px 的间隙「通过」,而字体一换就压上。)

    还没做的那一半:矮屏下令牌只有 50px 高,能用但小。真要做好得换排布
    (主帅移到两侧、战场居中),那是另一件事,不在这条里。
34. 🟢 ~~卡面绰号(37 张)~~ —— **2026-08-08 做完**。截图验过:「呂布 / 三姓家奴」
    在图鉴的小卡上放得下,没挤到身材。

    **占的是副名那一行,而且只在非双语模式下占。** 卡面是三行小布局(名字/副名/身材),
    那一行只有一个位置:双语模式下英文名是刚需(没它读不出这是谁),绰号让位;
    中文模式下那一行**本来是空的**,37 张卡因此白白少了一句最好记的东西 ——
    「三姓家奴」「獨眼龍」「常十萬」比本名更像一个人物。

    长度上限 `FACE_MAX = 5`:六个字的那两个(天下第一賢吏、滿洲第一勇士)**不上卡面**,
    但仍在表里、图鉴详情看得到。写成常量而不是直接删掉那两条 ——
    表是数据,长度是**版面**的约束,以后卡面改宽了改这一个数就行。

    ⚠️ 表是写死的常量(理由同 `defectors.ts`:`CardFace` 每帧都在渲染,
    import 那份 144KB 的 lore 会把首屏顶爆)。

    ⚠️ **`enPurity.test.ts` 抓到了我一次偷懒**:第一版让 `en` 回落到 `zh`,
    理由写的是「翻过去既失典故也失节奏」—— 闸门当场把 37 条全报了出来。
    它是对的:**发了 `en` 字段就该是英文**,回落读起来不是「保留了典故」,
    是「这里没做完」。逐条译了(獨眼龍 → One-Eyed Dragon、闖王 → The Dashing King),
    典故靠图鉴详情里的生平解释,不靠卡面那一行。

### E. 工程

35. 🟢 ~~卡池快照测试(防无意改动)~~ —— **2026-08-05 做完**。
    `src/content/pool-snapshot.json` 存每张卡的可读字段串(费用/攻/血 稀有度 主义 类型
    [关键词] fx:哈希),`poolSnapshot.test.ts` 逐张比对。守的是 `check-generated`
    覆盖不到的那一段:覆盖层是手写的、生成脚本不碰,而 check-generated 还需要姊妹仓库,
    在别人的机器和 CI 上一律跳过。摘要**只认影响对局的字段** —— 改名字、补风味句不红。
    故意的改动:`UPDATE_POOL_SNAPSHOT=1 npx vitest run src/content/poolSnapshot.test.ts`,
    **把快照的 diff 一起提交**,那份 diff 就是改动清单。
36. 🟢 ~~CSS 死样式检测~~ —— **2026-08-05 做完**。`npm run dead-css`,判定层
    `scripts/deadCss.ts` 带 25 条测试。全站 1201 个类名里报出 8 个。
    **动态取用(`styles[\`x${n}\`]`)的 11 份模块一律跳过 —— 那是盲区不是「干净」**,
    清单里单独列出来。是清单不是闸门(理由同 `price-cards`)。
41. 🟢 ~~`uiKit` 的按钮合并写完了但一个都没接上~~ —— **2026-08-06 接上了十五处**。
    原状是:三个变体都写好了却**没有任何一个屏 composes 它们**,`*Btn` 类名 41 个。
    `head` / `backBtn` / `title` 倒是十六个屏都接上了 —— 路子早就通,只是按钮那一半停在半路。

    **动手之前先装了一道闸门**(见 37):`composes` 在产物里是**两个平级的类**,
    谁赢只看谁写在后面。这一轮新增的二十几条「引过来再改两条」的覆盖,
    每一条都是靠打包顺序才生效的 —— 没有那道闸门,顺序一翻它们会**静默**全失效。

    接上的两批:

    - **六个 `.closeBtn` 其实是三样东西** —— 28px 方块 ×(功名簿/墓园/战绩簿)、
      整宽金色主按钮(每日一将/典籍)、整宽描边次按钮(每日军令)。
      同一个名字指三样东西,是 uiKit 想解决的问题最纯粹的形态。
      顺带查出两处是**抄漏不是设计**:战绩簿的 × 底色字色和另两个面板不一样,
      而且六个「关闭」里只有三个有键盘焦点圈 —— 焦点圈因此收进基件。
      方块收成 `.panelClose`,放在**结构**那一段(它不是「第四个变体」,
      是面板的结构件,和 `.head` 一类)。另四处 `goldBtn`/`plainBtn` 一并接上。
    - **九处胶囊里有四处逐字节相同** —— 数出来的分布本身就是结论:
      padding 九处九种写法、border/background/font-size 各四种、
      `-webkit-tap-highlight-color` **九处一处都没有**(手机上点下去会闪系统蓝框)。
      但乱斗规则钮 / 关底选卡组 / 构筑筛选 / 设置分区这四处连选中态都一模一样。
      **基件因此改成那一版,而不是我上一版照着截图估的那版** —— 凡是量得到的不该用估的。
      于是收下来是零视觉变化的纯去重(设置页改前改后逐像素相同)。

    结果:`uiKit` 从 dead-css 的清单里彻底消失(八个类全部有人用),
    顺序闸门的比对数 9 → 33 —— 它的覆盖面随这种写法一起涨,这是设计好的。

    **剩下的那五处是设计判断,留给你**:图鉴主义页签 / 列传筛选 / 典籍种类 /
    语言 / 关底视图页签。它们的选中态是**三套不同的写法** ——
    金填充深字(典籍、列传)、金渐变(语言、关底视图)、主义色(图鉴)。
    选哪一套统一(或者承认它们本来就该不一样)不是我该替你拍的。
    真要动,`.btnChipOn` 已经在基件里了,照上面四处的写法 composes 即可 ——
    `npm run css-dupes` 的胶囊那一段会把七处的取值分布直接列出来。

43. 🟢 ~~「同一个东西在各屏长得不一样」的检测~~ —— **2026-08-06 做完**。
    `npm run css-dupes`,判定层 `scripts/cssDupes.ts` 带 43 条测试。

    起因是上一条:那个「九处胶囊里四处逐字节相同」的发现是我**一个个 grep** 出来的。
    手工发现的问题不在于慢,在于**它不会再发生第二次** —— 下一个人加第十处胶囊时,
    没有任何东西会告诉他前面已经有九处了。

    报两样东西,含义完全不同:
    **跨文件逐字节相同的规则**(零风险去重机会)和**长相属性的取值分布**(漂移)。
    后者不能自动合(差异里混着有意的那部分),但分布本身就是结论:
    某个属性只有一两种写法说明它已经统一了;九处九种说明从来没人管过。
    **最有价值的一格往往是「几乎没人写」的那种。**

    第一次跑就抓到 **`-webkit-tap-highlight-color`:76 个方角按钮里 71 个没写、
    七处胶囊一处都没写**。手机上点下去会有一层系统高亮压在自绘的金框深底上,
    而且它出现在**手指底下**,玩家看不见自己点的是什么。
    这不是某一屏漏了,是没有一个人知道要写 —— 那种事只能在全局解决一次,
    现在在 `src/index.css` 的 `button {}` 里。

    两个刻意的取舍(改判据之前先读它们):
    - **从 TSX 认「真的挂在 `<button>` 上的类」**,不是只看 CSS。
      为了能键盘操作,这个仓库的卡片、格子、条目也都是 `<button>`,
      只看 CSS 的话 `.heroCard` / `.bookCard` / `.node` 会和「结束回合」混成一档。
    - **方角那七十多处只报种数,不逐条列。** 它不是一个族,列出来是七十行噪声,
      而一份读两次就没人读的清单比没有清单更危险。
      种数是体检指标(uiKit 立项时数出的「16 种圆角」就是它,按钮这档现在 11 种),不是待办。
37. 🟢 ~~闸门自检推广~~ —— **2026-08-06 补完**。纯函数判定层现在有**九份**,
    各带两个方向的自检:
    `campaignGate`(该红时红)· `simSeating`(先后手不许和座位绑死,含反向验证)·
    `firstPlayerGate`· `balanceGate`(含「猜拳局面」)· `baseline`(分得清噪声)·
    `perfBudgetGate`· **`contentRules`**(37 条)· **`cssOrder`**(40 条)· **`replayDiff`**(26 条,含变异测试)。
    做法可以照抄:把判定从脚本里抽成纯函数,再喂合成数据两个方向各验一遍,几毫秒跑完。

    `lint-content` 是最后补的,也是最该补的:12 条规则、24 处判断,**7 条 error 级**
    (`--strict` 下决定 CI 红不红),而此前一行测试都没有。
    测试**刻意不碰真卡池** —— 真卡池今天恰好没有某个错,不代表那条规则还活着,
    那正是闸门默默失效的方式。抽出前后输出逐字节相同(git stash 对拍过);
    把 `referencedId` 的 summon 分支故意打断,4 条当场变红。

    `cssOrder` 是**新装的一道**,不是抽出来的(`npm run css-order`,CI 接在 build 后)。
    起因是要给各屏的按钮接基件,而 `composes: btnGhost from '../uiKit.module.css'`
    在产物里**不是继承**,是给元素挂上两个**平级的类**:

        class="_btnGhost_zedng_87 _plainBtn_o3bay_293"

    特异度完全相同,所以同一个属性谁赢**只看谁写在后面**。
    于是「引公共基件、再覆盖自己那两三条」这套写法,整个建立在
    「打包器把 uiKit 排在使用方前面」这件今天恰好成立、但没人守着的事上。
    去 dist 里逐块量过:十三个含 uiKit 的分块里使用方都在后面,
    但 `LoreScreen` 那块的 uiKit 排在 7718 字节处、和其它十二块完全不一样 ——
    **这个顺序是逐块排出来的**,换打包器版本、动 manualChunks、把某屏改成同步 import
    都可能翻。翻了的后果是覆盖全部失效,而且不报错不崩,只是按钮悄悄变回基件尺寸。

    风险**随改动增长**:只引不改的时候顺序错了也没有可见后果,
    一旦开始「引过来再改 padding」,每条覆盖都是一个隐雷 —— 所以先装闸门再动按钮。
    测试刻意不喂真产物(今天的 dist 顺序恰好全对,只能证明「不该红时不红」),
    另把变异过的分块丢进 dist 跑过一遍,确认它真会红。
    输出里的「比对了 N 对」= **真正押了注的覆盖条数**,按钮那一轮做完从 9 涨到 33。

    **`replay-diff` 也抽出来了(2026-08-07),而且它并不像看上去那么「没有判断」。**
    它的结论是「两次跑出来的东西逐位相等」,所以一直排在最后。真去读发现两处判断,
    两处都能静默失效:

    1. **指纹取哪些字段。** `fingerprint` 现在是整份 `JSON.stringify(state)`,
       天然覆盖所有字段 —— 但这件事**没有任何东西守着**。谁哪天嫌它慢,
       改成只取 `{turn, phase, players}`,对拍照样全绿,而 `rng` 分叉、`nextIid` 分叉、
       新加字段分叉**全都查不出来**。指纹漏一个字段 = 闸门在那个维度上瞎了,没有任何征兆。
       所以那份测试是**变异测试**:逐个字段改一位,断言指纹必须跟着变。
       真把指纹改窄跑了一遍 —— **6 条当场变红**,含一条专门复现这种失效的用例。
    2. **多少条命令才算「真的对拍过」**(阈值 10)。开局就断的那种录不到几条,
       它「通过」了但什么都没验 —— 不合格算失败,不算通过。

    变异测试里那份字段清单**刻意逐条列出来而不是写循环**:循环会自动覆盖新字段,
    看起来更聪明,但那样就没人会来看这份清单,也就没人会问「这个新字段该不该进指纹」。

    另注:`balanceGate` 的文件头算清了「阈值配不配得上样本量」——
    结论是**配得上,不要把它也统一成 z 检验**,那只会让平衡闸门更难红。
42. 🟢 ~~身材对照卡~~ —— **2026-08-06 做完,而且结论比预想的重要**。
    `npm run sim-body` 造 26 张只差身材的白板对照卡各 800 局(合成卡只注入
    worker 的卡库,不进卡池不进快照,零风险)。本来只想要一个汇率,
    结果发现**线性身材模型是错的**:6 费同样 15 点身材,
    `14/1 → −18.9 · 8/7 → −1.4 · 1/14 → −12.1`,相差 17.5 个百分点,
    而「攻×1 + 血×0.8」对这三张的预测几乎相同。
    加一项 `min(攻,血)` 之后 z = 8.7、残差落到测量噪声以下。
    `bodyValue` 已改;拿卡池那份独立数据复验也同向(z = 2.93)。
    汇率一并拿到:**1 点 = 1.579 pp**,归组版据此落地(见第 3 节)。
    **`ai/greedy` 的 `unitValue` 故意没跟着改** —— 那是所有闸门的尺子,
    动它要重跑整套矩阵。两处从此不一致,`pricing.ts` 里留了注释说明。
46. 🟡 ~~AI 的身材评分也该带均衡项~~ —— **2026-08-06 试过了,更弱,已放弃**。
    做法本身是对的:`EvalWeights.balance` 混合比例(0 = 逐位退回旧式),
    对照在脚本侧构造(`A=normal:balance=1 B=normal npm run sim-ai-tiers`)。
    各 900 局:

    ```
    balance = 1     41.1%   z = −5.33   ← 明显更弱
    balance = 0.5   49.9%   z = −0.07   ← 没差别
    ```

    **一个在卡池上可证明更准的身材模型(z = 8.7),当成场面估值却让 AI 变弱。**
    不是矛盾,是两个问题不一样 —— 详见 `greedy.ts` 里 `EvalWeights.balance`
    那段注释(旋钮和结论都留着,免得下一个人再跑一遍 900 局)。
43. 🟢 ~~**坐斷東南贴着 40% 下沿**~~ —— **2026-08-08 做完,但这条待办的诊断是错的**。

    错在两处。一,**最低的不是坐斷東南,是克己復禮**(45.0% vs 46.8%)。
    二,更要紧的:把坐斷東南**去掉魏武那一格**之后,它对另外四套是 48/54/50/49,
    平均 50.25% —— 它一点都不弱,它的低分是魏武投下的一片阴影。
    真正超模的是 **魏武揮鞭 59.4%**,而且三个极端格(61/66/68)全是它造的。
    所以该动的是魏武,不是坐斷 —— 而且动魏武一次同时修好三格。

    **别照着「谁总分最低」去调牌**。总分是一行的平均,一格极端就能把它拖下来,
    读成「这套牌弱」会让人去加强一套本来平衡的牌。先把行拆开看。

    诊断过程里有两条值得留着:

    - **节奏假说被证伪**。骨架注释说「先手优势随卡组节奏放大」,顺着它会猜
      魏武是快攻。实测:魏武 vs 坐斷平均 25.5 回合(全表中位数),而且它
      **后手赢得更多**(73% > 65%),赢时还剩 21.5 血。它是靠身材磨,不是靠抢。
      现成的解释先去证伪,别拿来当结论。
    - **「削身材」会反向**。同一轮 A/B 里把李典(5/6)换成死戰之士(4/5),
      魏武反而 58.6% → **60.6%**。身材不是自由变量:定价体系保证省下的身材
      会被文本吃回去,削身材等于换一张不同的牌,不等于变弱。

    做法:魏武的抢攻从 2 张(張遼衝鋒 + 鄧艾突襲)减到 1 张,鄧艾 → 麥鐵杖。
    选它是因为坐斷東南是守护墙(12 张守护,5 张压在 6 费),突襲正是当回合拆墙的东西。
    实测 200 局/格:

    | | 改前 | 改后 |
    |---|---|---|
    | 魏武揮鞭 | 59.4% | 53.2%(z=2.8) |
    | 克己復禮 | 45.0% | 46.9% |
    | 坐斷東南 | 46.8% | 47.8% |
    | 坐斷 vs 魏武 | 34% | 39% |
    | 大隱 vs 魏武 | 32% | 43% |
    | 六套极差 | 14.4 | **6.9** |

    顺带一条**省机器时间的性质**:只改一套牌的牌表时,不含它的那 10 格
    在数学上一格都不会动,别家总胜率可以直接从那一格算出来。
    照这个算的六项预测与实测逐项差 ≤0.6 个百分点 —— 所以试候选时跑 5 个对位就够,
    不必跑满 15 个。

    **新闸门 `scripts/skeletonGate.ts`(9 条自检,`npm run deck-stats` 也会红)。**
    这道闸门的形状是被这次失衡逼出来的:魏武当时**每一项都合规** ——
    身材 216 比中位数只高 6.4%(容差 8%)、抢攻 2 张(骨架写「1~2 张」)、
    总攻 99(从来没有规则管过总攻)。所以「每项各设上限」这条路走不通,
    它已经每项都在限内了。判据只能是**有没有一套牌把各项之首占全了**:
    四项(总身材/总攻/抢攻/解场)里最多独占两项。并列不算占住 —— 优势不独有
    就不构成叠加。用序数不用 z 分数,是因为只有六个样本,z 的分母自己就抖。
    已验过它对**真实的改前牌表**会红且诊断逐字命中。
    注意:改后魏武是 2 项独占,**正好压在上限上,没有余量**。

44. 🟢 ~~**冒险第三章方差大**~~ —— **2026-08-07 压到 32 个点**(原 60:于謙 17% vs 文天祥 77%)。
    现在 70/65/68/63/63/38/43/38,前后半落差 21(闸门要 ≥8)。
    四关是 tune-campaign 网格搜的:安祿山 0.6→0.9(32%→65%)、于謙 0→0.75(17%→43%)、
    文天祥 0.45→0.9(77%→63%)、虞允文 0.45→0(73%→63%)。
    **tier→强度非单调这件事又一次是决定性的** —— 于謙 的曲线是
    `0:20% · 0.15:69% · 0.3:35% · 0.45:25% · 0.6:69% · 0.75:51% · 0.9:39%`,
    二分搜必然跑偏,只能网格。
    另注:ROADMAP 旧版写着「于謙每一档 deckTier 都在 81–100%」,那是**后手补偿之前**的,
    现在整条曲线都不一样了 —— 又一个「全局规则一动,先前的实测结论都要重量」的例子。
45. 🟡 **`sim-cards` 扫过 400 张了(2026-08-04),抓到三张真的超模** ——
    先读下面那段「怎么读 sim-cards」再动手。

    | 卡 | 60 局 | 600 局复测 | price-cards 有没有抓到 |
    |---|---|---|---|
    | 姜維(6 费 5/6 铁壁+冲锋,传奇) | +28.3 | **+29.2** | ✅ 卡面 25.1 ≈10 费(+4) |
    | 簡雍(2 费 2/1,战吼弹回一名敌将) | +16.7 | **+17.0** | ❌ 没上榜 |
    | 蘇飛(3 费 3/2,战吼全体敌将 2 点) | +20.0 | **+14.0** | ❌ 没上榜 |

    **姜維 两个工具都指认**,置信度最高;`+29.2` 是把 2 张换进桃園仁德后
    整套牌的胜率变化,这个量级已经不是「偏强」而是坏掉了。

    **2026-08-05 扫了 600 张(六个主义全覆盖),又确认六张**,全部 600 局复测过:

    | 卡 | 费 | 效果 | Δ | z | price-cards 怎么看 |
    |---|---|---|---|---|---|
    | 華雄 | 5 | 5/3 單挑,战吼本回合 +3/+0 | **+22.2** | 7.7 | **正好在曲线上** |
    | 曹安民 | 1 | 1/1,战吼偷一张牌 | +17.8 | 6.2 | +1 费 |
    | 候時而動 | 3 | 抉择:正午全体 3 点 / 抽 1+粮 2 | +17.7 | 6.1 | **正好在曲线上** |
    | 項伯 | 3 | 2/1 突襲,战吼全体敌将 2 点 | +16.7 | 5.8 | **正好在曲线上** |
    | 鄭厲公 | 3 | 3/2,战吼弹回一名敌将 | +15.5 | 5.4 | **正好在曲线上** |
    | 賈復 | 7 | 6/5 突襲,战吼徵召 1 | +15.3 | 5.3 | +1 费 |

    **2026-08-06 扫完了全池 2416 张**(60 局/张,101 分钟),榜首 6 张按 600 局复测,
    **六张全部站得住**(6 次比较的族错误率校正线是 z > 2.6):

    | 卡 | 费 | Δ | z | |
    |---|---|---|---|---|
    | 姜維 | 6 | **+29.2** | 10.1 | 与上一轮 600 局**逐位相同** —— 顺带是一次确定性对拍 |
    | **陳群** | 4 | **+23.0** | 8.0 | ⬅ 新 |
    | 華雄 | 5 | +22.2 | 7.7 | 复现 |
    | **神機營** | 6 | **+21.5** | 7.4 | ⬅ 新 |
    | 曹安民 | 1 | +17.8 | 6.2 | 复现 |
    | 候時而動 | 3 | +17.7 | 6.1 | 复现 |

    **2026-08-06:榜首两张查出来都是 bug,不是设计。**

    | 卡 | 病根 | Δ |
    |---|---|---|
    | 姜維 | pack3 定「7 费 6/6 鐵壁+衝鋒」,pack5 换成「6 费 5/6 + 抉择」清了 `battlecry` **没清 `keywords`** | **+29.2 → +19.2** |
    | 陳群 | pack3 定「光環:其他友方武將 +1/+0」,pack8 换成变形战吼并重写了 text **没清 `aura`** | **+23.0 → +14.3** |

    覆盖层是逐字段合并的,于是几样东西叠在了一起。第三例是嵇康
    (卡面文字说「戰吼:獲得潛行」,而它一上场就已经潜行)。
    闸门在 `src/content/overrideConflict.test.ts`,见坑 9。

    **所以剩下那几张动手前先查覆盖层** ——「这张卡为什么这么强」的答案
    有时候是「它根本不是设计成这样的」。两张都还偏强,但剩下那部分是
    设计问题(传奇带身材又带真战吼),那才是你要拍板的。

    **三张表去重之后,确认超模的一共十一张**(全部 600 局复测过):
    姜維 +29.2 · 陳群 +23.0 · 華雄 +22.2 · 神機營 +21.5 · 曹安民 +17.8 ·
    候時而動 +17.7 · 簡雍 +17.0 · 項伯 +16.7 · 鄭厲公 +15.5 · 賈復 +15.3 · 蘇飛 +14.0。

    #### ✅ 2026-08-07:十一张全部调平了(五轮实测收敛)

    现在 **+2.0 ~ +7.8**,改动全在 `src/content/overrides/tuning1.ts`,
    每一轮的实测轨迹都写在那份文件的头注里。

    **先验的尺子,再改的卡。** 「+10 到底算不算超模」需要一条基准线 ——
    否则也可能是**预组太弱**、任何能打的卡换进去都 +10,那样改卡就是在追幻觉。
    所以先量了五张中立白板武将(无关键词、无任何脚本)当对照组,各 600 局:
    `張布 +6.5 · 薛珝 +3.5 · 朱然 −1.0 · 陳餘 −1.2 · 辛毗 −4.8`,中位 −1.0。
    白板贴着 0 ⇒ 尺子没有正偏 ⇒ +10 是真的远。
    **收工线就按这个对照组的实际跨度定(−4.8 ~ +6.5)**,不是拍一个 ±4。

    量到的三条通则(以后调任何卡都能直接用):

    1. **`+1 费`第一格 ≈ −5.0pp** —— 七张纯涨费是同一个杠杆的七次独立测量
       (−8.1 / −7.3 / −10.3 / −3.4 / −2.2 / −2.1 / −1.9)。第二格只剩 −3.2,
       **6 费以上基本失效**:蘇飛 5→6 费一动不动(10.0 → 10.0)。
       高费段该削的是身材和效果,不是费用。
    2. **同效果的卡会停在同一个地板上。** 項伯 与 蘇飛 都是「全场 2 伤」,
       涨费削身材连推三轮都卡在 +7~+9 —— 贵的是那个效果本身。
       最后靠**把这一对拉开档次**才收住(蘇飛 改成 4 费的全场 1 伤)。
    3. **削过头会发生,而且只有量了才知道。** 華雄 第三轮同时削攻和削战吼,
       一步掉到 −2.3(比白板还差);第四轮把攻回调一格就回到 +2.2。
       「一次只动一个旋钮」在这里不是洁癖,是能不能读出因果的分界。

    ⚠️ **上面那句「这几张里有好几张在预组里」是错的 —— 一张都不在**(逐套查过)。
    `sim-balance` 跑完确认:与上次基线没有超过噪声的变化,矩阵纹丝不动。

    另外:全池扫完**没有一张越过 2416 次比较的校正线**(那条线要 |Δ| > 38.9),
    而 |Δ|>4 的有 1441 张 —— 纯随机下就该有约 1305 张。
    **这一档是噪声,别照着它改卡**,那正是这个脚本只配当筛子的原因。

    #### 🟢 2026-08-08:十一张在三条新全局规则下**又复验了一遍,一张都没动**

    这一天落了三条全局规则:粮尽 / 溃散 / 将星陨落。按第六轮那条教训全部重量
    (11 张 × 600 局,6 分钟):

    ```
    姜維 7.7 · 曹安民 7.5 · 項伯 7.3 · 陳群 5.8 · 賈復 5.0 · 鄭厲公 2.2
    神機營 1.8 · 簡雍 1.8 · 蘇飛 1.7 · 華雄 1.7 · 候時而動 −2.8
    ```

    和第六轮**逐条对得上**(差值全在 ±2.9 的噪声里),没有一张越过校正线(8.2)。

    **这次的结论和第六轮相反,原因值得记:全局规则不等于全局影响。**
    后手补偿改的是**每一局的开局**,所以它把整个卡池重新定了一次价;
    这三条都是**情境性**的 —— 要先有人断粮、先有人士气触底、先有传奇阵亡,
    大多数对局里一次都不触发。
    判断要不要重量,该问的不是「这条规则是不是全局的」,而是
    **「它在多少比例的对局里真的发生」**。不过这个判断只有量完才敢下,所以该量还是要量。

    #### 🟡 欠着一笔:全池扫描是**后手补偿之前**跑的

    2026-08-07 落地的后手补偿是一条**全局规则**,它改变所有卡的相对价值 ——
    十一张调平卡重量之后 姜維 就从 +7.8 弹回 +13.5(见 `overrides/tuning1.ts` 的第六轮)。
    同理,上面这段「没有一张越过校正线」也是旧规则下的结论,严格说需要重跑才算数。

    重跑命令(会写一份 DUMP,跑完可以直接喂 `fit-price` 重新校准定价表):

    ```bash
    SAMPLE=3000 GAMES=60 DUMP=<路径> npm run sim-cards
    ```

    **为什么这次没跑完:** 145,000 局,这台机器上按实测吞吐要 5 小时以上,
    而且全程会把别的验证拖慢一倍(单测从 38 秒涨到 115 秒)。
    更重要的是**它的产出这一程也用不上** —— 上一次全池扫描一张都没越过校正线,
    真正的信号是「榜首拿去 600 局复测」,那是又一轮多小时的活。
    所以这是一件**独立的、该单独安排一次的事**,不该夹在别的改动中间做半截。

    优先级判断:**不高**。已知的十一张已经逐张复验过了(十张仍在带内);
    这一跑找的是「补偿**新**造出来的超模卡」,而那一档在上一次扫描里本来就是空的。

    **两组独立的同形状效果各自落在同一区间**,这不是巧合:

    ```
    弹回一名敌将   簡雍(2 费) +17.0   ·  鄭厲公(3 费) +15.5
    全体敌将 2 点   蘇飛(3 费) +14.0   ·  項伯(3 费)  +16.7
    ```

    **结论:定价公式系统性低估节奏型战吼** —— 弹回手牌、群体点杀这类
    「不产生卡差但抢走对手一整个回合」的效果。八张确认超模的卡里,
    定价表只认为三张略高(各 +1 费),另外五张(**含最强的華雄 +22.2**)
    它评为**完全在曲线上**。

    ### 偏差已经按 op 量出来了(1200 张 / 60 局,`sim-cards` 的归组汇总)

    `price-cards` 的 `opValue` 至今是一张**拍出来的点数表** —— 注释写着
    「数值取自同类卡的实际定价」,也就是**从现状反推现状**:卡池里一整类效果
    定价偏低的话,这张表会忠实地把偏差学过来,再拿它去给新卡定价。
    按 op 归组打破了这个循环 —— 它用的是**对局结果**,不是卡池自身。

    | 效果 | 张数 | 平均 Δ | z | |
    |---|---|---|---|---|
    | `returnToHand` 弹回手牌 | 25 | **+10.1** | 6.3 | 偏低 |
    | `aoeDamage` 群体伤害 | 16 | **+9.4** | 4.6 | 偏低 |
    | `recruit` 徵召 | 7 | +8.8 | 2.9 | 偏低 |
    | `delay` 伏笔 | 19 | +4.3 | 2.1 | 偏低 |
    | `damage` 点杀 | **129** | +4.1 | 5.3 | 偏低 |
    | `tutor` 搜牌 | 52 | +3.5 | 2.8 | 偏低 |
    | `swapStats` 攻守互换 | 14 | **−5.8** | −3.1 | **偏高** |

    `gainArmor`(112 张,−0.3)· `heal`(63,−1.7)· `summon`(62,−1.1)·
    `draw`(104,+1.3)· `buffStats`(93,+1.3) 全部落在噪声里 —— **这些定价是准的**。

    模式很干净:**抢节奏与解场的效果被低估,加身材/回血/护甲/铺场的没有。**
    这正好解释了为什么八张确认超模的卡里五张被评为「在曲线上」。

    两个混淆做过对照,别再担心:
    - 全池 1200 张平均 Δ 只有 **+0.58** —— 不存在「有效果的卡整体强于白板」的抬升
    - z 相对**全池均值**算,不是相对 0 —— 基准只测一次,是所有 Δ 的共用常数项

    ⚠️ **别把「平均 Δ」直接当新分值。** 它量的是「带这个 op 的卡整体偏强多少」,
    不是「这个 op 本身值多少分」—— 一张卡常带好几个 op,归组时在每组各算一次。
    拿它定**方向与量级**,改完再扫一遍看 `price-cards` 的排序是否更贴近实测。
    **这是一次真正的校准,不是又一次拍脑袋。**

    这几张各自单独改、各自跑一轮闸门(不要一批一起上,教训见 A 节开头)。

    **2026-08-05:可测范围从 51.3% 提到 100%。** 基准从前写死桃園仁德(王道),
    而待测卡的主义必须与基准兼容 —— 于是霸道 294 / 名利 335 / 割据 210 /
    礼教 196 / 隐逸 142 共 **1,177 张专属卡一张都没量过**。
    现在按主义挑对应预组当基准,Δ 相对它自己那套算。
    中立卡仍固定用桃園仁德(保持与历史数字可比,且中立卡进哪套都合法)。

    ⚠️ **怎么读 sim-cards**:同一跑里榜首往下的那一长串**大部分是噪声**。
    400 张 / 60 局那次报了「偏离超过 ±4 的 235 张」,而纯随机下就该有约 216 张 ——
    两者几乎无法区分。原因是 Δ 是两次测量之差(60 局时标准误 ±9.1),
    而且做了 400 次比较。脚本现在按族错误率给校正线,并分两档报。
    **但「没越过校正线」不等于「里面没东西」**:上面那三张正是从没越线的榜首里
    复测出来的,而负向的三张(岳飛 −16.7 → −5.7、杜襲 −16.7 → −5.8、
    王離 −18.3 → −7.7)整齐地塌回去了 —— 典型的均值回归。
    **正确用法是两段式**:小样本扫全池排序 → 榜首拿去 600 局复测 → 复测后仍然大的才当真。

47. 🟢 ~~**`sim-history` 在 main 上是红的,而且一直是红的**~~ —— **2026-08-09 修完**,
    17 场全部落进 35–68% 带里,分布从 **36–88% 收到 47–64%**。

    **先纠正一句我自己写错的话**:2026-08-08 那轮结尾我把 sim-history 报成绿的。
    它不是。拉 `7a9d5ed`(本轮全部内容工作之前)出来实测,当时**红了八场**。
    先在的欠账,不是那一轮改出来的。

    ### 最要紧的一条:这道闸门自己是坏的,而且坏法是「会认错人」

    默认 `GAMES=60` 时单场标准误 6.5pp。同一份卡池与牌表:

    | | GAMES=60 | GAMES=240 |
    |---|---|---|
    | 番吾之戰 | **77% 出带** | **68% 在带内** |
    | 台州九捷 | **65% 在带内** | **78% 出带** |

    **两场都反了。** 不是「数字抖一点」,是名单本身错的 —— 照 60 局的读数去调参,
    会调一场本来没问题的,同时漏掉一场真有问题的。

    判据照抄 balanceGate 文件头那一套:看**带的半宽与标准误的比值**。
    带 ±16.5pp,60 局比值 **2.5**,240 局 **5.2**;而 balanceGate 那两道是 4.5 与 4.0,
    它把 4.0 当下限。60 局远在下限之下,再乘 17 场各判一次,**至少一场误红几乎是必然的**。
    **默认已改成 240**(32s → 123s)。一道会随机翻红的闸门比没有更糟。

    ### 两个旋钮各自的脾气(调之前先读)

    - **`deckTier` 非单调,而且是极端非单调**。笠澤那一行:
      `0:92% · 0.15:100% · 0.3:81% · 0.45:83% · 0.6:61% · 0.75:92% · 0.9:94%`。
      只能网格搜。原因也清楚:tier 不是强度旋钮,它是**取池的分位**,
      挪一档换掉的是一批具体的牌,而那批牌之间未必有强弱序。
    - **`hp` 多数时候是弱旋钮,有时是空旋钮**。笠澤三档 hp 逐格几乎不动(92/92/92);
      而**睢陽之戰的三行 hp 逐格完全相同** —— 它是 `objective: survive 14`,
      玩家靠撑满回合取胜,敌方主帅血量根本不进胜负判定。
      `tune-history` 现在会检出「这一轴零方差」并直说是空旋钮(按数据判,不按 objective 硬编码)。

    ### 工具

    新增 `npm run tune-history`(`ONLY=<id,…>`,网格搜 tier × hp,只打印不改代码),
    以及 `workers/history.worker.ts` —— sim-history 与 tune-history **共用它**,
    理由同 campaign.worker:两处各留一份 `play()`,改一处忘了另一处的表现是
    「调参搜出来的数落库之后闸门不认」,两边都自洽,极难查。
    顺带 sim-history 并行化,134s → 31s(同局数下与串行**逐个数字相同**)。

    ### 落库的九处

    | 场次 | hp | tier | 240 局 |
    |---|---|---|---|
    | 長平之戰 | 42 | 0.80 → **0.00** | 65% → 47% |
    | 番吾之戰 | 42 | 0.82 → **0.90** | 68% → 50% |
    | 邯鄲之戰 | 44 | 0.90 → **0.75** | 36% → 49%(这场是贴**下**沿) |
    | 垓下之戰 | 52 → **60** | 0.50 → **0.90** | 77% → 57% |
    | 赤壁之戰 | 40 → **48** | 0.72 → **0.45** | 88% → 53% |
    | 睢陽之戰 | 55 | 0.35 → **0.00** | 68% → 53% |
    | 黃天蕩之戰 | 52 | 0.30 → **0.60** | 85% → 64% |
    | 鄱陽湖之戰 | 44 | 0.90 → **0.60** | 87% → 54% |
    | 台州九捷 | 40 | 0.75 → **0.45** | 78% → 52% |
    | 笠澤之戰 | 36 | 0.70 → **0.60** | 79% → 64% |

    ### 第三、第四根轴:量过了,大部分是死的

    tier × hp 用尽之后又量了开局态势(`tune-history` 加了 `EARMOR=` 轴换掉 hp 轴)。
    结果推翻了我自己的假设 —— **敌方开局护甲几乎是空旋钮**:

    | | 甲=2 | 甲=4 | 甲=8 | 甲=12 | 甲=16 |
    |---|---|---|---|---|---|
    | 笠澤(tier 0.60) | 61% | 61% | 63% | 64% | 61% |
    | 黃天蕩(tier 0.60) | 58% | 58% | 58% | 56% | 56% |

    八倍护甲换来零。道理事后很清楚:**护甲是一次性缓冲,而对局跑二十五回合** ——
    十四点甲还不到后期一个回合的场面伤害。真正有分量的是**每回合都在**的东西。

    照这条重挑,黃天蕩 的水寨从一座加到两座:**64% → 58%**(0/4 守护是身材,不是缓冲)。
    顺带更贴 `situation` 里那句「一堵极难凿穿的墙」—— 八千人困十万金骑四十八日。

    ⚠️ **笠澤 64% 调不动,四根轴全量过了,别再试**(细数记在卡表的注释里)。
    其中一次是**试了又撤回**的:拿掉玩家那张额外起手牌只值 1pp,和噪声分不开,
    而它从玩家手里拿走了真东西 —— **换不到可测的收益就不该留着**。

    结论不是「还没找到旋钮」,是**这套旋钮的粒度就这么粗**:
    tier 是唯一有分量的一根,而它只有七个离散取值,取到哪档就是哪档的胜率;
    档与档之间没有可以微调的东西。63~64% 就是 0.60 那副牌的落点,它在带内。
    再往下压得动主公技或换 doctrine —— **那是重做这一关,不是调参。**

48. ✅ **矮屏回合横幅压住我方战线**(2026-08-09 查实,当日修完)

    **落点改成由战场几何算出来** —— `src/ui/useBannerPlacement.ts`。
    判据是纯函数 `src/ui/bannerPlacement.ts`,7 条单测双向验过(见铁律 11)。

    **为什么百分比一定解不了(算术,不是手感)。** 这一处按视口百分比挪过三轮
    (38% 压敌方那排 → 48% 压我方那排 → 40%),每一轮都是「这一档让开了,
    那一档压上」。量完四档的净空(两排令牌之间的净空)就清楚了:

    | 视口 | 净空 | 横幅高 | 结果 |
    |---|---|---|---|
    | 740x360 | **16.6px** | 18.4px | 竖着**根本装不下** |
    | 844x390 | 34.9px | 18.4px | 装得下 |
    | 900x420 | 39.4px | 16.8px | 装得下 |
    | 926x428 | 36.7px | 17.6px | 装得下 |

    一个固定百分比要同时落进 16.6px 和 39.4px 两条带子的正中,无解。

    现在的规则两条分支:
    - 净空够 → 坐进正中(那也是间隙最大的位置)。实测余量 6.9 / 7.1 / 8.3px。
    - 净空不够 → **横着让开**,滑进战线左右较宽的那条空档,仍坐在楚河汉界线上。
      360 高的机器走这条(安卓横屏最常见的一档),实测余量 92.8 / 117.8px。

    走过的两条死路仍然记着,免得重走:
    - 塞进 `.battlefield` 按它的中心定位 —— 结构对(两排的中点就是它的垂直中心),
      但那个容器 z-index 只有 2,而手牌/HUD 在 40~60,会被压住。
    - **无条件**挪到左侧留白 —— 空间够的档位上没必要把提示赶去角落。

    ⚠️ 高屏(> 430px)**故意不接管**:1024x768 下横幅仍压着两排各 5.6 / 17.2px。
    那是定过的桌面观感,大屏上一块 1.4 秒的居中提示压一点没人抱怨,
    而按几何让位会把它推到左侧空档。要改是设计决定,不是修 bug。

    三个查这个 bug 时踩到的坑,每一个都值一条:
    1. **`getBoundingClientRect()` 不是布局量**。第一版拿它去量令牌,五档全红 ——
       令牌入场动画正在跑,量到的是某一帧。改用 `offsetTop/offsetHeight`
       沿 offsetParent 链累加:它是布局量,动画一概不影响,而且基准
       (offsetParent 的 padding box)和绝对定位的 `top/left` 本来就是同一套坐标。
    2. **算一次不够**。横幅活 1.4 秒,这期间字体、立绘、手牌都还在改高度,
       而战场是 `flex: 1` —— 上下任何一块变高两排就跟着挪。加了 ResizeObserver。
    3. **`data-reduced-motion` 只喂 CSS**。`useFlip` 读的是 `useSettings.reducedMotion`,
       默认 false 且**不跟随系统偏好**,所以钉那个属性关不掉 JS 侧的 FLIP 变换。
       e2e 改成播种设置项。

49. ✅ **e2e 残余不稳**(2026-08-09,`landscape` 这条已根治)

    `landscape.spec` 原来在 `--workers=8 --repeat-each=8` 下会偶尔红。
    根治的办法不是加等待,是**把探针装进页面里**:逐帧记录横幅与最近令牌的间隙,
    取它**一生中的最小值**。于是测试不再依赖「外面跑得多快」,
    而且断言从「某一瞬间不压」变成了**存在期间一直不压** —— 是更强的那条。
    现在 `--workers=8 --repeat-each=12`(72 次)全绿,整套 81 条 0 flaky。

    **查这类问题的五个已知层次**,按根子从浅到深(全都在 landscape 上踩过):
    1. 睡固定时长去等一个**会自己消失**的元素(横幅 1400ms 后被摘掉,窗口只有 200ms)
    2. 带着入场动画量矩形 —— 量到的是某一帧,而是哪一帧取决于机器多快
    3. 字体从回退切到正式,度量一变,`clamp(…vh)` 排的东西整排挪位
    4. **夹具本身没钉住** —— 点「第一个挑战」拿到的是**每日轮换**的题,
       于是红不红取决于日期。昨天绿今天红而代码一行没动,这是最难查的一种。
    5. **在外面等、进去量一次**:等到了不代表量得到。会消失的东西要在页面内采样。

    第 4 条是通用教训:e2e 里任何「第一个 / 最新的 / 今天的」都是隐形随机源。

    ⏳ **剩下的那一类查清了根因(2026-08-09,受控实验)。**
    不是用例的问题,是**测试在和 vite dev server 抢时间**:
    dev 下模块是**按请求现转**的,而 e2e 的断言最密的地方恰好是「点进模式 → 进对局」,
    正是那一屏第一次被转换的时刻。

    | 情形 | 结果 |
    |---|---|
    | 刚改过源码 / 刚启的 server | 4~11 条要靠重试才过,3.9~6.5 min |
    | 同样那几条**单独**跑 | 5 条全过,22 秒 |
    | 紧接着再跑一次(模块热了) | **81/81,2.6 min** |

    报错永远是 `toBeVisible() failed` / `element(s) not found` / `click timeout`,
    从来不是「内容不对」那一类 —— 这也是判断它不是回归的依据。

    **✅ 修了(`e2e/global-setup.ts`)。** 开跑前沿 vite 重写过的 import 说明符
    爬一遍模块图,把转换开销先付掉。实测:

    | 冷启动的做法 | flaky | 时长 |
    |---|---|---|
    | 什么都不做 | 4 | 3.9 min |
    | vite `server.warmup` | **7**(更糟) | 4.1 min |
    | globalSetup 爬模块图 | 1 → 0 → 0 | 3.1 / 2.8 / 2.8 min |
    | (对照:模块已热) | 0 | 2.6 min |

    ⛔ **`server.warmup` 是条歪路**:它在**服务端进程里**转换,而 Playwright
    探到端口能响应就开跑了 —— 不是把开销挪走,是让测试去抢一个正忙的 server 的 CPU。
    关键在「**开跑之前**」这四个字,而 globalSetup 是唯一能保证这一点的地方。

    **为什么爬模块图而不是用浏览器点一遍各模式入口:** 点按钮等于把用例的选择器
    再抄一份,文案一改预热就悄悄不工作了,而表现只是「e2e 又开始偶尔红」。
    爬的是 dev server 本来就会被要求转换的那批东西,不碰任何界面细节。
    286 个源文件爬到 279 个,4.3 秒。

    ⚠️ 预热自己也有自检:爬到的模块少于 150 就**当场红**(实测验过 —— 把入口改名,
    整套立刻失败并说明「修它,别删它」)。一道会默默停止工作的预热比没有更糟。

    还没做的另一条路:让 e2e 跑**构建产物**(`vite preview`)而不是 dev server ——
    没有现转这回事,顺带更贴近玩家拿到的东西。⚠️ 但产物会注册 Service Worker,
    缓存行为会掺进每一条用例里,得先想清楚(`check-offline` 那道闸门就是为它存在的)。

50. ✅ **六张卡带着一条恒等于空操作的 `draw: 0`**(2026-08-09 做完)

    王允 · 孫匡 · 張英 · 陶應 · 劉範 · 楊復恭,战吼都是
    `[{gainSupply:1}, {draw:0}]`。**卡面没有说谎**(文案只写「屯糧 +1」),
    所以玩家看不见;但它会被按名字数进 `draw` 组给定价校准掺沙子。

    在 `cards.ts` 的合并层剥掉了(`src/content/noOp.ts` 的 `stripNoOps`),
    和 `applyBattleFixes` / `applyTuning` 同一条链 —— 那本来就是「改不动生成层
    时在这里改」的地方。判据 `isNoOp` 从 `scripts/pricing.ts` 搬进 `src/`,
    三处共用一份(定价不算它 / lint 报它 / 合并层剥它),不再抄第二份。

    **只对生成层用。** 手写卡包改得动,该让 `lint-content` 直接报,
    不该被这一层替它们擦干净。

    顺带查实的三件:
    - `lint-content` 的 `no-op` 规则**只扫可收集卡**,衍生物一张都看不见。
      新的 `noOp.test.ts` 扫全池,当场多抓出一张 `token-liu-yan`。
    - 那张是**故意的**:「謠言」整张牌就是洗进对手牌库的废牌(卡面写「空無一物」),
      而引擎要求锦囊必须有一段脚本,于是拿 `draw 0` 当那段。测试里列成明写的例外。
    - ⛔ **别把 `overrides/` 也归进 content chunk**(量过,反效果)。动机合理
      —— perf-budget 那条基线的标签写着「卡池 + 覆盖表」,而规则只收
      `generated/`,覆盖表一直在首屏主包里。但实测:主包 189.8 → 109.9,
      内容层 128.7 → **233.1**,合计 390 → 411.5KB,超总预算 11.5KB。
      gzip 是按 chunk 各压各的,分开反而各自有更合身的字典。

51. 🟡 **首屏主包贴着上限:189.8 / 190 KB**(2026-08-09)

    余量只剩 **0.2KB** —— 下一次任何改动都可能把 `perf-budget` 变红。
    这不是第 50 条造成的(它只占 0.6KB,之前也才 0.8KB 余量),
    是主包被各模式的内容数据长期顶上来的。
    基线注释写的是「留 15% 余量,免得改一行文案就红」,而现在是 0.1%。

    **✅ 顺手修掉的那一半:这道闸门原来能被一行分块规则刷绿。**
    查这条时随手在 `manualChunks` 里加了一行,主包 189.8 → **75.7 KB**,
    而新出来那块 116.6 KB **首屏照样下载** —— 三条基线一条都不匹配,
    于是它不受任何约束,报表上却像是瘦了一半。总预算那条拦住了它
    (合计几乎没变),但按 chunk 钉基线的本意正是回答「谁胖了」。
    现在要求**每个首屏文件都被某条基线覆盖**,新增一块就得先说清它该多大。
    顺带两处:同一条基线匹配到多个文件要**合起来算**(原来只量第一个);
    `problems` 改成带 `kind` 的结构 —— 原来 perf-budget 那边是
    `filter(p => p.startsWith('chunk'))`,新加的那一类**判对了却被打印层丢掉**,
    闸门照样绿。按前缀猜字符串分类迟早这样坏。

    **剩下的那一半(真正省字节的):把各模式的内容数据懒加载。**
    用 sourcemap 归因量过主包里最大的几块:

    | 源文件 | 原始 | 谁在急加载路径上拽着它 |
    |---|---|---|
    | `content/campaign.ts` | 61.8 KB | `campaignStore` / `bossRushStore` / `expeditionStore` / `tower.ts` |
    | `content/historyBattles.ts` | 35.4 KB | `historyStore` |
    | `content/lethalPuzzles.ts` | 11.9 KB | `lethalStore` |
    | `content/relics.ts` | 9.7 KB | `expeditionStore` |

    ⚠️ **别照着 61.8+35.4 去估收益。** 试着把这五个文件单独分块时,
    rollup 把**整个卡池**(signature 65.7 / signature-skills 37 / rivals 23.3 /
    bonds 14.1 / 各 pack 覆盖表…共 ~250KB)一起搬了过去 —— 主包掉的 114KB
    大半是卡池换了个文件装。真实收益要等 import 真的变成动态的才量得准,
    粗估 25~35 KB gzip。

    结构上的根子是一句话:**TitleScreen 为了显示「群雄逐鹿 12/32」这样的进度角标,
    急加载了每个模式的 store,而每个 store 又在模块顶层 import 自己那份内容数据。**
    TitleScreen 自己只用到 `BOSSES.length` / `HISTORY_BATTLES.length` 两个数,
    但 store 的 action(开一关、结算一阶)确实要数据,所以不是把两个常数抽出来就完事。

    **✅ 第一刀已经开完:`historyStore`(2026-08-09)**

    | | 主包 | 合计首屏 |
    |---|---|---|
    | 之前 | 189.8 / 190 | 387.0 / 400 |
    | 之后 | **180.3** / 190 | **377.5** / 400 |

    真减 9.5 KB gzip(不是换个文件装 —— 合计跟着一起降了),余量 13.0 → 22.5 KB。

    做法**不是**把 store 改成异步,而是查清楚它到底要什么:
    `historyStore` 从 35.4KB 的名局定义里只取三样 —— id 存不存在、发多少功勋/卡包、
    有没有逆位/分歧点。牌组、修正、态势文案一个字段都没碰。
    于是抽出 `content/historyIndex.ts`(18 行,一场一行),store 与标题页只引它;
    重的那一份现在只有懒加载的三屏(名局/斩杀/讲堂)还引着。

    ⚠️ 手写的投影会烂,所以 `historyIndex.test.ts` 逐场逐字段**双向**对拍:
    多一场、少一场、奖励改了、新加了逆位而索引没跟上,四种都红。
    没有这道闸门就别抽索引 —— 走样的表现是「标题页分母少一场、通关少发一个卡包」,
    全是不崩不红的那一类。

    **✅ 第二、三刀:`lethalStore` 与冒险那一家(2026-08-09)**

    | 这一刀 | 主包 | 合计首屏 |
    |---|---|---|
    | 起点 | 189.8 / 190 | 387.0 / 400 |
    | `historyStore` | 180.3 | 377.5 |
    | `lethalStore` | 177.7 | 374.9 |
    | 冒险那一家 | **161.9** | **359.1** |

    合计从主包上剥掉 **27.9 KB gzip**,余量 0.2 → 40.9 KB。

    冒险这一刀动了四处,因为 `campaign.ts` 是被**四家**拽着的:
    - `campaignStore` / `bossRushStore` / `expeditionStore` → 改引 `campaignIndex.ts`
      (32 关的 id、奖励、试炼奖励;顺序也是数据 —— 解锁判定问的是「这一关排第几」)
    - `tower.ts` 里的 `towerDeck()` 拆去 `towerDeck.ts` —— 那三行是它唯一用到
      `campaign.ts` 的地方,而 `towerFloor()` 是纯数学。`towerStore` 只为读一个
      `rewardMerit` 就把 61.8KB 拽了进来。

    ⚠️ **这一刀让 e2e 的冷启动更容易翻红**:数据挪到路由入口才加载,
    而那正是 dev server 现转最贵、e2e 断言最密的一步。不是回归(单独跑全过,
    热了之后 81/81),详见第 49 条那张表。

    **✅ 第四刀:`expeditionStore` 的 `relics.ts`(2026-08-09)** —— 主包 161.9 → **160.2**。
    只要 id + 稀有度做加权抽取,名字/说明/开局修正都是懒加载那一屏才用的。
    ⚠️ 这一份的**顺序影响抽取结果**:`offerRelics` 的 LCG 按数组顺序累加权重,
    顺序一变,同一个 rngState 抽到的是另外三件 —— 进行中的远征在玩家眼前换了一批宝物,
    而且没有任何东西会报错。`relicIndex.test.ts` 逐位钉住。

    **五个 store 都收完了。** 主包 189.8 → **160.2**(−29.6 KB gzip),
    首屏合计 387.0 → **357.4**,余量 0.2 → **42.6 KB**。

    ⚠️ **顺带查实一件与代码无关、但会让人误判的事:`npm test` 会因为机器负载变红。**
    CPU 被别的程序占到 93% 时实测:6 条最吃 CPU 的用例超时,而且 vitest 会报
    `[vitest-worker]: Timeout calling "onTaskUpdate"` —— 那是一条 **unhandled error**,
    于是**所有用例都绿、退出码却是 1**,耗时从 36 秒涨到 130 秒。
    机器闲下来(19%)再跑就是 1490/150、36.4 秒、退出 0。
    看到「全绿但 exit 1」先看负载,别去翻代码。

    ⛔ 重新分块救不了,已经量过两次:见第 50 条末尾(overrides)与本条上半(modes)。

---

## 3. 已经查实做不了的(**别再试**)

这些都量过,写在这里是为了不让下一个人重做一遍:

| 项 | 结论 |
|---|---|
| 表字覆盖率 | **53.3% 是天花板**。传里出现「字」的 877 人只漏 7 个,那 7 个确实没有表字。 |
| 生卒年 | **50.6% 是天花板**。从生平再抠只多 7 条。 |
| 出战台词 | 源头只有 taunt/ult 两类各 147 人,**已挖尽**。 |
| 具名兵器 | 源头生平里 **0 条**(泛指的「弩」「矛」只有 18 条)。坐骑 4、身高 8、容貌 8。 |
| 生平简→繁转换 | **不能做**。48 个简体字里只有「内」一个是繁體中不用的,其余(征 后 面 于 云 岳 范 巨 松…)本身就是合法繁體字 —— 转了会把「征荊州」变成「徵荊州」、「岳飛」变成「嶽飛」。 |
| 繁简折叠对事迹抽取 | 实测只多 **4 次**命中,收益全在补词表。别把功劳记在它头上。 |
| 1 费重复度 | 70% 有双胞胎,**降不下去** —— 1 费只有 3 点身材预算,买不起第二样东西。唯一的路是 0.3 点的免费微效果(`payFor` 的扣款门槛是 0.4)。 |
| 举荐 / 子嗣 / 出仕年龄 / 谥号 | 分别只有 22 / 22 / 3 / **0** 条,撑不起一栏。 |
| **兵种相克** | **做完了,测出来不行,撤了。** 见下。 |
| **AI 用「均衡」身材估值** | 做完了,**更弱** —— 900 局 41.1%(z = −5.33)。旋钮留着默认关,见待办 46。 |
| **用实测回归出定价权重** | 40 个参数的岭回归喂不动,**别再试**。但**按效果归组那条路是通的,2026-08-06 已落地**,见下。 |

### 兵种相克为什么撤

骑→弓→步→骑,攻击方 +1 伤害。引擎、测试、文案全写完,闸门不过:

```
坐斷東南       43.6% → 39.0%   掉出 40–60%
魏武揮鞭 vs 坐斷東南   71%      冲破 30–70%
```

原因是结构性的 —— 六套预组的兵种构成是**这条规则出现之前**定死的:

```
魏武揮鞭  騎11 步8 謀2 弓1     ← 步8 打进对面骑9,吃满加成
坐斷東南  騎9 謀6 步5 弓2      ← 步只有 5,反过来吃不回来
```

这条规则不改任何一张卡,却白送了魏约 4.6 个百分点。而预组骨架是跨很多轮
`sim-balance` 手调出来的,极其敏感(颜真卿 4/6 → 4/4 一张牌就能把矩阵打出闸门)。

**推广到一条通则**:任何**全局战斗规则**(相克、地形、天气减伤)都有这个性质 ——
它不改任何一张卡,却改变所有卡的相对价值,而相对价值正是预组调了很多轮的东西。
这类改动**要么和预组重调一起做,要么做成 opt-in**(挂 `FieldRule` 或乱斗规则,
双方同吃且不进天梯与关底)。

### 定价表第一次被实测量过了(2026-08-05)—— 回归走不通,归组走得通

`price-cards` 的点数表是个闭环:它的注释说数值「取自同类卡的实际定价」,
也就是**从现状反推现状**。卡池里某一类效果整体定价偏低时,这张表会忠实地把偏差
学过来,再拿去给新卡定价 —— 用它自己量自己,结果永远是「都在曲线上」。

唯一在环外的东西是对局结果:

```
npm run sim-cards      # DUMP=<路径> SAMPLE=3000 扫全池落盘实测 Δ(≈3h)
npm run fit-price      # DELTAS=<那个文件> 拟合 + 留出集裁决
npm run synth-deltas   # 造真值已知的假数据,排练上面那道判决
```

#### 结论一:当前这张表**指对了方向**

全池 2416 张 × 60 局。「卡面价值超出本费用档多少点」与「实测胜率 Δ」的秩相关
**ρ = +0.208(z = 10.4)**。这是这张表第一次有外部证据 —— 它不是瞎猜。

#### 结论二:40 个参数的岭回归喂不动,**别再试**

先排练再读数(坑 8)。植入已知真值之后:

| 每张卡的噪声 | 回归能不能把真值捞回来 |
|---|---|
| ±0.2pp | 能。damage 2.5→2.51、returnToHand 6.0→5.62、tutor 5.0→4.76,样本外 ρ +0.061(z = 12.5) |
| ±1pp | 能(z = 3.0) |
| ±2pp | 开始输(z = −2.2) |
| ±9pp | 系数飞到 ±30。砍到 8 个、4 个,甚至「神谕」直接指定真正被植入信号的那 4 个 —— 照样输 |

而 60 局/张的实测噪声正是 ±9pp。真实数据上它的表现完全一致:**身材系数解出来是负的**
(「身材越好的卡越弱」),脚本的守卫当场拦下、拒绝换算权重。

#### 结论三:按效果**归组**那条路是通的,而且过了留出集

回归要同时解 40 个未知数,每个都吃满 ±9pp。归组只问「带这个 op 的卡整体偏强多少」——
那是 n 张卡的均值,噪声按 √n 缩小(returnToHand 有 51 张,均值标准误只有 ±1.0pp)。
代价是它**分不开同卡的多个 op**,但混杂只让估计有偏,不让它爆炸。

规则:|z|>2 的 op 才动;「一点卡面价值值多少胜率」由单变量回归从数据里读
(实测 ≈ **1.0pp/点**,而不是以前拍的「大约 3pp」);改动量 = (该 op 均值 − 全池均值) / 斜率。
**选 op、定尺度、定改动量三步全部只用一半的卡**,另一半从头到尾没参与:

```
留出集 1272 张   当前表 ρ 0.220  →  归组版 ρ 0.278
                 Δ +0.058,Steiger 相依检验 z = 2.95   ✓
```

实测出来的偏差很有规律:

| | op | 实测 Δ |
|---|---|---|
| **被低估**(抢节奏 / 解场 / 过牌) | returnToHand +10.0(z=8.9,全表最强)· aoeDamage +8.6 · seize +16.4 · destroy +9.8 · recruit +7.4 · delay +6.3 · damage +4.0(n=247, z=7.0)· reduceCost +3.7 · tutor +3.4 | |
| **被高估**(加身材 / 回血 / 护甲 / 铺场) | swapStats −4.6 · borrow −4.8 · gainMana −2.2 · discover −1.5 · gainSupply −1.4 · heal −1.1 · summon −1.0 · gainArmor −0.8 | |
| **原来就定得准** | draw(209 张)· buffStats(182 张)· freeze · silence · stealCard 全落在噪声里 | |

#### 结论四:身材的价值**不是线性的** —— 而这是卡住整件事的那颗钉子

第一次照实测改完,`price-cards` 反而变难用:可疑名单从 200 张涨到 574 张
(8.3% → 23.8%),榜首整页都是低费伤害锦囊。根子是**只量了效果,没量身材** ——
效果整体涨了三倍,而费用曲线(由以身材为主的中位卡决定)几乎没动。

于是造了 26 张只差身材的白板对照卡各 800 局(`npm run sim-body`,合成卡只注入
worker 的卡库,不进卡池不进快照)。本来只想要一个汇率,结果**发现线性模型是错的**:

```
6 费,同样 15 点身材总量:
   14/1 → Δ −18.9      8/7 → Δ −1.4      1/14 → Δ −12.1     ← 相差 17.5 个百分点
```

而「攻×1 + 血×0.8」对这三张的预测几乎相同 —— 它把这整块结构丢进了残差
(±4.74pp,而测量噪声只有 ±2.50pp),两个斜率的 z 都掉到 1.0,
**看起来像「测不出来」,实际是模型错了**。加一项 `min(攻,血)`:

```
Δ = 0.174·攻 + 0.551·血 + 2.500·min(攻,血) + 费用档     z(均衡) = 8.7
残差 ±2.17pp —— 已经落到测量噪声以下
```

道理很直白:1 血的怪碰谁死谁,1 攻的怪换不掉任何东西,两头都是废的。
**均衡本身就是身材的一部分。** 拿卡池那份完全独立的数据复验也同向(z = 2.93)。

#### 落地了

`bodyValue` 换成带均衡项的形式(乘 0.6334 把全池均值对齐旧式,这次只改形状),
二十一个效果权重按归组规则改。汇率用 sim-body 的 **1 点 = 1.579 pp**,
而不是拿本份数据回归出的混合斜率(那又是闭环)。

留出集裁决:**ρ 0.255 → 0.325(Δ +0.070,z = 4.50)**,可疑名单 203 → 388 张(16.1%)。

**已经收敛了**:把同一条规则再喂给校准后的表,留出集 Δρ −0.010(z = −1.68)——
没有进一步的改进空间了。也就是说这条路已经走到头,再往前要么加样本量
(60 局/张的噪声是 ±9pp),要么换方法。

路上修掉两个自己的错,都记在提交信息里:**归组的均值是「每张卡」,而
`aoeDamage`/`damage` 这些权重是乘在点数上的** —— 直接加等于又乘了一遍平均点数
(aoeDamage 一度被顶到 7.2/点);以及**我先拿带 bug 的规则过了留出集才修 bug**,
已回退重跑。

⚠️ **这把尺子是贪心 AI**,它对防守向(治疗/护甲/屯粮)和靠**选择**产生价值的
(发现/借将)系统性低估(铁律 8)。上面那串负号里有一大半正好落在这两类上。

⚠️ **`ai/greedy` 的 `unitValue` 故意没跟着改成带均衡项** —— 那是所有闸门的尺子,
动它要连基线一起重跑。所以现在 AI 仍然高估极端身材的单位(待办 46)。

数值和证据都写在 `scripts/pricing.ts` 的 `DEFAULT_WEIGHTS` 头上,每个 op 后面标着
它的 n 和 z;`npm run fit-price` 会打出完整的建议表,并且**同时报「这张表让
price-cards 报出多少张可疑」** —— ρ 量排序,那一列量「报表还读不读得下去」,
只看 ρ 会把工具优化成没人读得完的样子。

---

## 4. 这个仓库特有的坑(踩过的)

1. **改引擎类型跑 `npm run build`,不是 `tsc --noEmit`** ——
   后者只走根 tsconfig,test/server/drivetest 三份配置是 build 才检的。
   vitest 不做类型检查,所以会出现「982 个单测全绿、只有构建红」。

2. **静默失效是这个仓库最贵的一类 bug。** 它们不崩不红,只是「有东西不见了」。
   已知的几类各有一道源码级闸门(见 `src/**/*.test.ts` 里带「闸门」字样的注释):
   - 新 `GameEvent` 要补 eventText / floats / useEventAnimations 三处
   - 新 `PlayerState` / `GameState` 字段要补裁剪层两处(否则**单机全对、联机全无**)
   - 新关键词要补讲堂词条
   - `Record<string, T>` 覆盖表**撞键 tsc 不报错**,后写的整条盖掉先写的
   - 显示层直接读生成层会**看不见手写补遗**(列传屏就这么漏过几轮)

3. **覆盖表里的手写内容必须是原文里的原话。** `src/content/dossier.test.ts` 会去断言
   籍贯 / 结局 / 绰号 / 镇守 / 归附 都能在那个人的生平里原样找到。照抄可以,推断不行。

4. **先量再写。** 凭印象排出来的清单有一半是空的 —— 上一轮八项估计错了五项,
   而且都是高估(估 200 实测 10、估 40 实测 0)。写抽取规则之前先写十行探针量一遍。

5. **偶发红先怀疑数据不一致,别当 flake 放过** ——
   `arena.test` 三次红一次那回,根因是两张卡撞了 id。
   但确定性种子的长测试(fuzz / replay)单独重跑能过,那就是并行跑重活时的超时。

6. **这个仓库可能有另一个会话同时在改。** 提交用
   `git commit -F - -- <明确路径>` 而不是 `git add -A` ——
   前者只提交列出路径的工作区状态,和索引里有什么无关,免疫竞态。

7. **把教训写进注释 ≠ 修好了。** 2026-08-04 一天之内撞见三次同一个模式:
   有人发现了问题、把结论工工整整写进注释、然后代码照旧。
   - `sim-campaign` 的「60 局撑不起自己的判定」写在 `campaign.ts` 顶部 ——
     脚本没改,默认还是 60,CI 还钉着 `GAMES: 60`,于是 main 上挂着一次假红,
     而 ROADMAP 把那次误报当成了待办第一条。
   - `sim-hero-mirror` 的「first 必须显式传入,不能从 seed 推」写在 `sim-balance.ts` 顶部 ——
     隔壁文件一字不差又写了一遍那个 bug,还带着歪尺子劝退了两轮主公技设计。
   - `price-cards` 的 `opValue` 没有 default,新 op 加进引擎时没人回头补表。
   **修法一律是可执行的断言,不是第三条注释。** 判定逻辑抽成纯函数 + 合成数据双向验证,
   几毫秒跑完(样板见 `scripts/campaignGate.test.ts`)。

8. **先验证尺子,再读数。** 对称的模拟有个免费且极强的自检:
   **两边放同一个东西,结果必须是 50%**。
   `sim-hero-mirror` 从来没做过这一步,于是带着 26% 的中性点跑了很久 ——
   六个备选主公里四个被误判「过弱」,而唯一「合格」的那个才是真正出界的。
   任何对镜/镜像类模拟上线前先跑这一关(`npm run sim-firstplayer` 顺带就是它)。

9. **覆盖层是逐字段合并的 —— 「换掉一版设计」要把该清的字段全清掉。**(2026-08-06 加)
   全卡池最超模的那张卡(姜維,Δ +29.2)不是设计出来的,是这么合出来的:
   pack3 定「7 费 6/6,鐵壁 + 衝鋒」,pack5 要换成「6 费 5/6 + 抉择」,
   清了 `battlecry`、**漏了 `keywords`** —— 三样东西全叠上了,还便宜 1 费。
   同一类事故让嵇康的卡面文字在说谎(写着「戰吼:獲得潛行」,它一上场就潜行)。

   **tsc 抓不到**(两层都是合法的 `Partial<CardDef>`),**lint-content 也抓不到**
   (每个字段单看都没问题)。它只在**语义**上露馅:
   姜維 抉择的一路是「获得守护」,而它已经带着冲锋和铁壁 ——
   **自相矛盾的卡面就是覆盖没清干净的指纹。**

   闸门在 `src/content/overrideConflict.test.ts`,**三条**,合起来盯住
   「卡面写的」与「卡面做的」不一致的三种形态:

   | | 症状 | 2026-08-06 抓到 |
   |---|---|---|
   | 自相矛盾 | 卡面已有的关键词,效果里又给自己加一次 | 姜維 · 嵇康 |
   | **做了没说** | 卡面有这个机制,文案一个字没提 | 25 处,已全部补完 |
   | **说了没做** | 文案开头点名一个关键词,而卡上没有 | 凌統 · 句扶 · 孫臏 |

   最后那一类更坑 —— 玩家照卡面预期它能马上出手,实际不能,会导致误判。
   三张的病根都是「写了 text 忘了写 `keywords`」,而这个 bug 一直在**削弱**它们:
   补上之后 凌統 −3.3 → +0.7、孫臏 −6.8 → −0.8,全回到中线。
   **补关键词是加强,补之前先跑 sim-cards。**

   改一张卡的设计时,先 `grep` 那个 id 看有几层覆盖定义过它。

   **顺带查实的两件**(2026-08-06,别再重做):
   - **卡面数字是准的。** 扫了 1225 处「文案里的数」对「脚本里的量」,
     对不上的只有 6 处,而那 6 处**文案才是对的** —— 病在脚本。
   - 那 6 处是 `draw count=0`,**恒等于什么都不做**(`drawCards` 的循环体一次都不跑,
     不掉血不发事件)。对局里无害,但它会被**按名字数进 `draw` 组**,
     给定价校准掺沙子。现在 `lint-content` 报 warn(`no-op` 规则)、
     `pricing.opsOf` 跳过它。根因在生成层,**没有素材源改不了**,
     所以只报不修 —— 有素材源的机器上修好生成器,这几条会自己消失。

   顺带:**「这张卡为什么这么强」的答案有时候是「它根本不是设计成这样的」** ——
   查超模卡先看覆盖层,再谈数值。

10. **CSS Modules 的 `composes` 不是继承,是挂两个平级的类。**(2026-08-06 加)

    ```css
    .plainBtn { composes: btnGhost from '../uiKit.module.css'; padding: 4px 11px; }
    ```

    产物里是 `class="_btnGhost_zedng_87 _plainBtn_o3bay_293"` ——
    两个**单类选择器**,特异度完全相同。所以 `padding` 谁赢
    **只看它们在样式表里谁写在后面**,而那个顺序是**打包器逐个 chunk 排出来的**。

    今天十三个含 uiKit 的分块里使用方都在后面(所以覆盖都生效),
    但 `LoreScreen` 那块的 uiKit 排在 7718 字节处、和其它十二块完全不一样 ——
    这说明它是排出来的,不是保证。翻过来的后果是覆盖**静默**全失效:
    不报错、不崩、控制台干净,只是十几个屏的按钮悄悄变回基件的尺寸。

    闸门是 `npm run css-order`(要先 build,CI 接在 build 后)。
    另外两条要记住的:

    - **伪类会打破平局。** `.btnGhost:hover:not(:disabled)` 是 (0,3,0),
      本地的 `.plainBtn` 只有 (0,1,0) —— 所以想覆盖悬停态,本地也得写伪类。
      基件里 `.btnChip:hover` 压 `.btnChipOn` 就是这么撞上的
      (选中的胶囊被鼠标扫过会变回灰色),那条得单独写一遍。
    - **只引不改的类在产物里根本不存在**(空规则被压掉了)。
      所以 `css-order` 报的「比对了 N 对」永远小于 composes 总数,
      而那个 N 才是**真正押了注的覆盖条数**。

11. **掐掉跑模拟的终端 ≠ 掐掉模拟。**(2026-08-07 加)

    `parallelMap` 起的是 worker 线程,它们活在 `node` 进程里,而那个进程
    **比启动它的 shell 活得久**。中断终端(或工具层的 TaskStop)只杀掉了 shell,
    node 还在满负荷跑,只是没人再看它的输出了。

    后果不是「浪费一点 CPU」,是**后面每一跑都被拖垮**,而且看不出原因:
    实测一次 `sim-cards`(9 张 × 600 局)正常是 **4 分半**,
    在两个僵尸进程的背景下跑了 **100 分钟还没完** —— 中途我一度怀疑是
    某张卡造出了跑不完的对局,还去二分排查了一轮。
    真相是机器只剩 0.2 个核给它。

    症状:`Get-Process node` 里有**你不认识的、CPU 还在涨**的进程。
    处理:`Stop-Process -Id <pid> -Force`,确认 CPU 停了再重跑。
    别急着怀疑代码 —— **先确认这台机器是不是在替你跑上一轮**。

12. **加一张卡会改掉 Boss 卡组 —— 所以内容包也得跑 `sim-campaign`,不只是 `sim-balance`。**
    (2026-08-08 加,当天就是被这一条咬的)

    这两道闸门看的是**两种完全不同的卡组**:

    | | 卡组从哪来 | 加新卡会不会动它 |
    |---|---|---|
    | `sim-balance` | 六套**手写预组**(`decks.ts`) | 不会 —— 除非你把新卡写进预组 |
    | `sim-campaign` | `bossDeck()` 从**全池**确定性挑 30 张 | **会** —— 新卡分够高就顶掉一张老卡 |
    | `sim-history` | `battleDeck()`,而它**就是 `bossDeck()`** | **会**,而且同样容易被漏掉 |

    所以「这几张新卡一张都不在预组里,sim-balance 不受影响」是对的,
    但它**不蕴含**「战役不受影响」。我上一包就是照这个推理只跑了 sim-balance,
    第二十六卡包推上去之后 sim-campaign 直接 exit 1。

    **这一条我只学了一半,值得写下来(2026-08-09 补)**:当时补上了 sim-campaign,
    却没想到 `sim-history` 走的是**同一个** `bossDeck`(`battleDeck` 只是它的一行包装)。
    实测本轮前后对拍:14 场名局的敌方牌组**全部**变了,每场换掉 1~4 张。
    结论不是「再加一道闸门」,是**认出这条依赖的形状** ——
    凡是从全池自动组牌的东西,加卡都会动它。现在这样的有三处(战役 Boss、名局、逆位),
    以后再加第四处,同一句话还得再说一遍。

    具体经过值得记,因为它顺带查出了一个真的洞:
    輜重營(4 费 3/6 守護)在 `bossDeck` 的打分函数眼里是 4 费档最划算的一张,
    于是被选进 Boss 卡组 —— 而**那个打分函数根本不认识 `supplyCost`**,
    在它看来军需是白送的身材。选进去之后 Boss 付不出粮,那张牌一直躺在手里,
    岳飛那一关的玩家胜率 47.5% → **63.7%**(z=3.6),第 2 章的难度曲线当场被抹平。

    修的是**自动组牌**,不是那三张卡:军需要求「攒到 N 点粮再打」,
    而 Boss 走纯贪心 AI(铁律 8),这类牌交给它就是纯粹的减法。
    `bossDeck` 现在过滤掉所有 `supplyCost` 卡,并有一条断言守着
    (`campaign.test.ts`,报出来会直接点名是哪张卡)——
    那条断言比重跑一次 sim-campaign 快四个数量级,而且它说得出**为什么**红。

    另注:`sim-campaign` / `sim-balance` **每跑一次都会把结果写成新基线**。
    所以「与上一次基线相比没有变化」只说明和**上一跑**一样 ——
    想知道某个改动干了什么,得自己把改动摘掉再跑一次对拍,不能指望那行提示。

---

## 5. 常用命令

```bash
# 闸门(有明确 exit code,CI 在跑)
npm run build            # 类型检查 + 构建
npm test                 # 单测
npm run lint-content     # 卡池结构性自检
npm run perf-budget      # 首屏体积(总量 + 按 chunk 基线)
npm run css-order        # composes 的层叠顺序(要先 build)—— 覆盖失效是静默的
npm run replay-diff      # 引擎确定性对拍
npm run check-offline    # PWA 断网可玩
npm run check-generated  # 生成层漂移(没有素材源时自动跳过)

# 环境(几秒,先跑这个)
npm run doctor           # 这台机器上什么能跑、什么不能、为什么(附闸门耗时表)

# 平衡(**下结论一律用默认局数或更高**)
#
# 六道全部并行(worker 池,见 scripts/parallel.ts),耗时为 10 线程实测。
# 跑一遍全套:**13 分钟**(串行要 41 分钟)。
#
# ⚠️ sim-history 是 2026-08-09 才补进这张表的 —— 在那之前它不在表上,
#    于是没人跑它,于是它红了很久没人看见。**加了新闸门就把它加进这张表。**
npm run sim-campaign     # 冒险 24 关难度曲线            ~4min   (串行 12min)
npm run sim-history      # 18 场名局难度                 ~2min   (串行 9min)
                         #   默认 GAMES=240 —— 60 局时这道闸门会认错人,别改小
npm run sim-firstplayer  # 先手优势 + 对镜类模拟的仪器自检 ~2.5min (串行 5min)
                         #   AI=tiers 换尺子 · COMP=sweep 试算后手补偿
npm run sim-cards        # 单卡边际胜率(SAMPLE=3000 扫全池 ≈3h,实测 47s/张·单线程)
                         #   CONTROL=1 量对照组 —— 白板武将的 Δ 分布就是这把尺子的零点
                         #   CONTROL=1 BASE=<0..5> 逐套量零点(六套中位一致,跨度差三倍)
                         #   VERIFY=<局数> 榜首自动复测 · DUMP=<路径> 落盘给 fit-price
npm run sim-hero-mirror  # 备选主公技对镜                ~1.7min (串行 8.5min)
npm run sim-balance      # 六套预组互搏矩阵              ~1min   (串行 5min)
                         #   跑完自动与上次基线对比
npm run tune-campaign    # 关底 deckTier 网格扫描(只打印建议,不改代码)
npm run tune-history     # 名局 deckTier × hp 网格扫描(同上,ONLY=<场次 id>)
                         #   EARMOR=2,8,16 把第二根轴换成敌方开局护甲(多半是空旋钮)
                         #   会检出「这一轴零方差」并直说是空旋钮(睢陽的 hp 就是)

#   JOBS=1 退回单线程 —— 怀疑并行接错时用它对拍,走的是同一份代码
#   JOBS=N 指定线程数;默认 核数-2,最多 16
#
# **并行不影响任何一个数字**:每局的种子只由索引决定、引擎是纯函数,
# 所以按索引装回去就与串行逐位一致。三道接入时都做过并行/串行对拍。

# 定价(不卡门,只记录)
npm run price-cards      # 全池卡面价值 vs 费用曲线 —— 唯一扫得动全池的尺子
npm run synth-deltas     # 造真值已知的假数据,排练下面那道拟合
npm run sim-body         # 身材的价格 —— 造只差身材的白板对照卡(零风险)
npm run fit-body         # 换个模型重新分析 sim-body 的落盘数据,不重跑对局
npm run fit-price        # 拿实测 Δ 量定价表准不准(DELTAS= 指向 sim-cards 的 DUMP)
                         #   同时报「这张表让 price-cards 报出多少张可疑」—— 两个指标都要看

# 界面(不卡门,只记录)
npm run dead-css          # 死样式清单(动态取用的模块会跳过,那是盲区)
npm run theme-audit       # 主题就绪度:还有多少写死的颜色挡着亮色主题(561 挡路 / 1359 不挡路)
npm run css-dupes         # 同一个按钮在各屏长得不一样 —— 逐字节重复 + 取值分布
npm run css-order         # 上面那道闸门;输出的「比对了 N 对」= 真正押了注的覆盖条数

# 盘点(不卡门,只记录)
npm run audit-generals   # 武将档案覆盖率 + 机制重复度
npm run audit-en         # 英文漏翻
npm run audit-zh         # 繁简覆盖

# 内容重新生成(**需要 ../ThreeKingdomMastersIOS**)
npm run import-content
```
